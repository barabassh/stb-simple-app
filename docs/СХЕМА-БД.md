# Схема базы данных — этапы 1–4

PostgreSQL 16, миграции через Prisma. Ниже — целевая схема этапа 1 (разделы 1–6),
правила, по которым она расширяется (раздел 7), модели этапа 2 (раздел 8), этапа 3 (раздел 9)
и этапа 4 (раздел 10).

## 1. Перечисления

```prisma
enum Role {
  ADMIN        // администратор
  MANAGER      // менеджер
  EMPLOYEE     // сотрудник
  CONTRACTOR   // подрядчик
}

enum AuditAction {
  LOGIN
  LOGIN_FAILED
  LOGOUT
  CREATE
  UPDATE
  DELETE
  ROLE_CHANGE
  PASSWORD_CHANGE
  STATUS_CHANGE
  SESSION_REVOKE
  EXPORT
  IMPORT
}
```

## 2. Пользователь

```prisma
model User {
  id                 String    @id @default(cuid())
  login              String    @unique              // хранится в нижнем регистре
  fullName           String
  position           String?
  email              String?   @unique
  phone              String?
  comment            String?

  passwordHash       String
  role               Role      @default(EMPLOYEE)
  isActive           Boolean   @default(true)

  lastLoginAt        DateTime?
  failedLoginCount   Int       @default(0)
  lockedUntil        DateTime?

  createdAt          DateTime  @default(now())
  updatedAt          DateTime  @updatedAt
  createdById        String?
  updatedById        String?

  sessions           Session[]
  auditLogs          AuditLog[] @relation("AuditActor")

  createdBy          User?     @relation("UserCreatedBy", fields: [createdById], references: [id], onDelete: SetNull)
  createdUsers       User[]    @relation("UserCreatedBy")
  updatedBy          User?     @relation("UserUpdatedBy", fields: [updatedById], references: [id], onDelete: SetNull)
  updatedUsers       User[]    @relation("UserUpdatedBy")

  @@index([role])
  @@index([isActive])
  @@index([fullName])
}
```

**Пояснения к решениям:**

- `login` приводится к нижнему регистру **до записи**. Уникальный индекс
  PostgreSQL чувствителен к регистру, поэтому без нормализации `Ivanov` и
  `ivanov` окажутся разными учётными записями. Нормализация выполняется в
  схеме Zod, а не в базе.
- Пользователь не удаляется — ни жёстко, ни мягко, поэтому поля `deletedAt`
  у модели нет. Доступ закрывается деактивацией (`isActive = false`), запись
  со всей историей остаётся (`ТЗ.md`, п. 4.7). Ссылки из журнала аудита и из
  будущих таблиц (автор проекта, ответственный за смету) не разрушаются, а
  логин и email навсегда закреплены за одним человеком: уникальные индексы
  действуют среди всех записей, включая неактивные.
- `email` приводится к нижнему регистру в схеме Zod по той же причине, что и `login`.
- Признака обязательной смены пароля нет: пароли назначает и сбрасывает только
  администратор, пользователь свой пароль не меняет (`ТЗ.md`, п. 3.3).
- `failedLoginCount` и `lockedUntil` реализуют требование ТЗ п. 3.3 —
  блокировка на 15 минут после 5 неудач.
- Связи `createdBy`/`updatedBy` объявлены как `SetNull`, как и `AuditLog.actor`.
  Пользователи не удаляются, поэтому на практике ссылки не обнуляются; читаемый
  логин автора в журнале всё равно хранится отдельно (см. ниже).

## 3. Сессия

```prisma
model Session {
  id           String    @id @default(cuid())
  userId       String
  tokenHash    String    @unique          // SHA-256 от токена, сам токен не хранится
  expiresAt    DateTime
  lastActiveAt DateTime  @default(now())
  ip           String?
  userAgent    String?
  createdAt    DateTime  @default(now())
  revokedAt    DateTime?

  user         User      @relation(fields: [userId], references: [id], onDelete: Cascade)

  @@index([userId])
  @@index([expiresAt])
}
```

В базе лежит только хэш токена: утечка дампа базы не должна давать возможность
войти под чужой сессией. Сессия считается действительной, если
`revokedAt = null` и `expiresAt > now()`.

Отзыв сессий происходит при деактивации пользователя, сбросе пароля
администратором и по кнопке «Завершить сессию». Истёкшие записи пользователя удаляются при его
входе, перед созданием новой сессии (`createSession` в `src/lib/auth/session.ts`).

## 4. Журнал аудита

```prisma
model AuditLog {
  id         String      @id @default(cuid())
  at         DateTime    @default(now())

  actorId    String?
  actorLogin String                        // дублируется строкой намеренно
  action     AuditAction
  entity     String                        // "User", "Session", "Project", ...
  entityId   String?
  summary    String                        // читаемое описание для списка
  changes    Json?                         // [{ field, before, after }]
  ip         String?
  userAgent  String?

  actor      User?       @relation("AuditActor", fields: [actorId], references: [id], onDelete: SetNull)

  @@index([at])
  @@index([entity, entityId])
  @@index([actorId, at])
  @@index([action])
}
```

**Ключевые решения:**

- `actorLogin` хранится строкой рядом с `actorId`: запись журнала читается
  сама по себе, без соединения с таблицей пользователей (`ТЗ.md`, п. 4.9).
- `actorId` необязателен: события `LOGIN_FAILED` возникают до того, как известен
  пользователь, — там заполняется только `actorLogin` с введённым значением.
- `entity` + `entityId` делают журнал универсальным: проекты и сметы следующих
  этапов подключаются без изменения таблицы.
- `changes` — массив в `jsonb`. Отдельная таблица изменений дала бы более строгую
  структуру, но журнал только пишется и читается целиком, поэтому дополнительные
  соединения и индексы не нужны.
- Записи журнала не изменяются и не удаляются. В приложении нет ни одного пути,
  который бы их правил.

### 4.1 Что запрещено записывать

Значения полей `passwordHash`, `password`, `newPassword`, `tokenHash` в `changes`
не попадают ни при каких условиях. Список задаётся явной константой в
`src/lib/audit`, а не эвристикой по названию поля: правило «поле содержит
`password`» рано или поздно пропустит `secretKey` или `apiToken`.

Сброс пароля журналируется как факт: `action = PASSWORD_CHANGE`,
`changes = null`.

### 4.2 Вычисление изменений

Изменения вычисляются сравнением состояния «до» и «после» в пределах одной
транзакции с самим изменением. Поля, значения которых не поменялись, в `changes`
не попадают. Ссылочные поля пишутся читаемым значением (не идентификатор роли,
а её название), иначе журнал бесполезен без ручной расшифровки.

### 4.3 События этапа 1

| Событие | `action` | `entity` / `entityId` | `changes` |
|---|---|---|---|
| Вход | `LOGIN` | `Session` / новая сессия | — |
| Неудачная попытка входа | `LOGIN_FAILED` | `Session` / — | — |
| Выход | `LOGOUT` | `Session` / завершённая сессия | — |
| Создание пользователя | `CREATE` | `User` / пользователь | заполненные поля |
| Изменение данных, в том числе своего профиля | `UPDATE` | `User` / пользователь | изменённые поля, кроме роли и статуса |
| Смена роли | `ROLE_CHANGE` | `User` / пользователь | `role` |
| Деактивация, активация | `STATUS_CHANGE` | `User` / пользователь | `isActive` |
| Сброс пароля | `PASSWORD_CHANGE` | `User` / пользователь | — |
| Завершение сессии администратором или самим пользователем в профиле | `SESSION_REVOKE` | `User` / владелец сессии | — |
| Выгрузка в Excel, PDF или печатное представление | `EXPORT` | `User` — реестр пользователей, `AuditLog` — журнал аудита / — | — |

- У `LOGIN_FAILED` поле `actorId` пустое, `actorLogin` — введённый логин; причина отказа
  (логин не найден, учётная запись деактивирована, неверный пароль, блокировка) видна
  только в `summary` — форма входа по-прежнему показывает единый текст.
- Одно сохранение формы, изменившее и данные, и роль, и статус, даёт до трёх записей
  в одной транзакции: фильтр журнала по `ROLE_CHANGE` находит смену роли независимо
  от того, каким экраном она сделана. Сохранение без изменений ничего не пишет.
- Действие, которое ничего не изменило (повторный отзыв уже завершённой сессии),
  в журнал не попадает.
- Текущую сессию в профиле не завершают: это выход, он пишется как `LOGOUT`.
  «Завершить все, кроме текущей» даёт одну запись `SESSION_REVOKE`.
- Вкладка «История изменений» в карточке пользователя — записи с `entity = "User"`
  и `entityId` этого пользователя (право `users.history.read`); входы и выходы видны
  в журнале по автору.
- `summary` читают и те, кому IP-адреса недоступны, поэтому в нём нет IP-адресов:
  завершённая администратором сессия названа только браузером.
- `summary` выгрузки называет отчёт, формат и число строк: «Выгрузка «Реестр
  пользователей» (Excel), строк: 12». Выгрузка, отклонённая по правам, не пишется —
  данные никому не переданы.

## 5. Диаграмма связей

```
User ──< Session          отзываемые сессии пользователя
User ──< AuditLog         действия, совершённые пользователем (actorId, SetNull)
User ──> User             createdBy / updatedBy (SetNull)
```

## 6. Начальные данные

Сид (`prisma/seed.ts`) создаёт единственную запись: администратора `admin` с
паролем из переменной окружения `SEED_ADMIN_PASSWORD`. Пароля по умолчанию в коде нет — при отсутствии
переменной сид завершается с ошибкой. Повторный запуск сида не создаёт дубликат.

## 7. Правила расширения схемы

Соглашения, обязательные для всех моделей следующих этапов:

1. Идентификатор — `String @id @default(cuid())`.
2. Обязательные поля: `createdAt`, `updatedAt`, `createdById`, `updatedById`.
   Их отсутствие означает, что отметку «кем и когда изменено» придётся
   добавлять задним числом по всей базе.
3. Удаление — мягкое, через `deletedAt`, если на запись могут ссылаться другие
   таблицы или журнал. Исключение — пользователи: они не удаляются вовсе,
   только деактивируются (раздел 2).
4. Денежные суммы — `Decimal @db.Decimal(14, 2)`. `Float` для денег недопустим.
5. Даты хранятся в UTC; перевод в `Europe/Kyiv` выполняется при отображении.
6. Любое изменение схемы оформляется миграцией
   (`npx prisma migrate dev --name ...`). Применённые миграции не редактируются.
7. Новая сущность сразу подключается к аудиту: значение `entity` — имя модели.

## 8. Этап 2: сведения о компании

Требования — `ТЗ.md`, раздел 5.

### 8.1 Перечисления

```prisma
enum LegalForm {
  EENMANSZAAK
  VOF
  CV
  BV
  NV
  STICHTING
  VERENIGING
  COOPERATIE
  MAATSCHAP
  OTHER
}

enum CompanyAddressType {
  OFFICE      // адрес офиса (vestigingsadres), ровно один
  POSTAL      // почтовый адрес, не больше одного
  WAREHOUSE   // склад, до 10
}

enum SocialNetwork {
  LINKEDIN
  FACEBOOK
  INSTAGRAM
  X
  YOUTUBE
  TIKTOK
  OTHER
}
```

### 8.2 Модели

```prisma
model CompanyProfile {
  id                  String     @id @default(cuid())
  singleton           Boolean    @unique @default(true)   // запись одна, см. пояснения

  legalName           String                               // statutaire naam
  tradeName           String?                              // handelsnaam
  legalForm           LegalForm?
  registeredOn        DateTime?  @db.Date                  // datum inschrijving
  statutorySeat       String?                              // statutaire zetel

  kvkNumber           String?                              // 8 цифр
  establishmentNumber String?                              // vestigingsnummer, 12 цифр
  rsin                String?                              // 9 цифр
  vatId               String?                              // btw-id
  vatNumber           String?                              // omzetbelastingnummer
  payrollTaxNumber    String?                              // loonheffingennummer

  email               String?
  phone               String?                              // основной, +31684614732
  website             String?
  activityDescription String?
  postalSameAsOffice  Boolean    @default(true)

  version             Int        @default(1)              // защита от одновременной правки

  createdAt           DateTime   @default(now())
  updatedAt           DateTime   @updatedAt
  createdById         String?
  updatedById         String?

  createdBy           User?      @relation("CompanyProfileCreatedBy", fields: [createdById], references: [id], onDelete: SetNull)
  updatedBy           User?      @relation("CompanyProfileUpdatedBy", fields: [updatedById], references: [id], onDelete: SetNull)

  addresses           CompanyAddress[]
  phones              CompanyPhone[]
  socialLinks         CompanySocialLink[]
  activities          CompanyActivity[]
}

model CompanyAddress {
  id                  String             @id @default(cuid())
  companyId           String
  type                CompanyAddressType
  name                String?                              // название склада
  street              String?                              // пусто у абонентского ящика
  houseNumber         Int?
  houseNumberAddition String?
  postbus             String?                              // номер абонентского ящика, только POSTAL
  postcode            String?                              // пусто у недозаполненного склада
  city                String?
  country             String             @default("NL") @db.Char(2)   // ISO 3166-1 alpha-2
  sortOrder           Int                @default(0)

  createdAt           DateTime           @default(now())
  updatedAt           DateTime           @updatedAt
  createdById         String?
  updatedById         String?
  deletedAt           DateTime?

  company             CompanyProfile     @relation(fields: [companyId], references: [id], onDelete: Cascade)
  createdBy           User?              @relation("CompanyAddressCreatedBy", fields: [createdById], references: [id], onDelete: SetNull)
  updatedBy           User?              @relation("CompanyAddressUpdatedBy", fields: [updatedById], references: [id], onDelete: SetNull)

  @@index([companyId, type])
}

model CompanyPhone {
  id        String         @id @default(cuid())
  companyId String
  label     String?
  number    String?
  sortOrder Int

  company   CompanyProfile @relation(fields: [companyId], references: [id], onDelete: Cascade)

  @@index([companyId])
}

model CompanySocialLink {
  id        String         @id @default(cuid())
  companyId String
  network   SocialNetwork?
  url       String?
  sortOrder Int

  company   CompanyProfile @relation(fields: [companyId], references: [id], onDelete: Cascade)

  @@index([companyId])
}

model CompanyActivity {
  id          String         @id @default(cuid())
  companyId   String
  sbiCode     String?
  description String?
  isMain      Boolean        @default(false)
  sortOrder   Int

  company     CompanyProfile @relation(fields: [companyId], references: [id], onDelete: Cascade)

  @@unique([companyId, sbiCode])
}
```

У `User` появляются обратные связи `CompanyProfileCreatedBy`, `CompanyProfileUpdatedBy`,
`CompanyAddressCreatedBy`, `CompanyAddressUpdatedBy`.

### 8.3 Пояснения к решениям

- **Обязательно только `legalName`** (`ТЗ.md`, п. 5.4). Адрес офиса и почтовый адрес
  записываются, только если заполнены целиком (это проверяет схема Zod); склад и строки
  телефонов, соцсетей и видов деятельности сохраняются такими, как введены, поэтому их поля
  в базе допускают `NULL`. Полностью пустые строки не сохраняются.
- **Одна запись.** Уникальное поле `singleton` с единственным допустимым значением `true`
  не даёт создать вторую запись даже при двух одновременных первых сохранениях: второе
  получит нарушение уникальности, а не дубликат. Значение `true` закрепляется ограничением
  `CHECK ("singleton" = true)`, которое дописывается в SQL миграции, созданной с
  `--create-only`, до её применения — Prisma такие ограничения не описывает.
- **Один офис и один почтовый адрес.** Частичный уникальный индекс, тоже дописанный
  в SQL миграции: `UNIQUE ("companyId", "type") WHERE "type" IN ('OFFICE', 'POSTAL')
  AND "deletedAt" IS NULL`. Склады ограничиваются десятью в схеме Zod.
- **Адреса — отдельные записи с полями аудита и мягким удалением.** На склады будут
  ссылаться проекты и поставки следующих этапов, поэтому адрес при правке обновляется
  на месте (форма передаёт его `id`), а удалённый склад получает `deletedAt`. Выборки
  исключают записи с `deletedAt`.
- **Телефоны, социальные сети и виды деятельности — части записи о компании**, а не
  самостоятельные сущности: на них ничто не ссылается, и меняются они только вместе
  со всей формой. Поэтому при сохранении они заменяются целиком в той же транзакции,
  без собственных `createdAt`/`updatedById` и без `deletedAt`, — это сознательное
  исключение из правил 2 и 3 раздела 7. Кто и когда их изменил, видно по отметкам
  `CompanyProfile` и журналу, где каждый список записан «было → стало».
- **`version`** увеличивается при каждом сохранении с изменениями. Форма отправляет
  версию, с которой открылась; обновление выполняется условием
  `where: { id, version }` и при нуле обновлённых строк возвращает ошибку одновременной
  правки. Сравнение по `updatedAt` не используется: у отметки времени есть точность,
  а у счётчика — нет.
- **`registeredOn` — только дата** (`@db.Date`), без времени и часового пояса: переводить
  её в `Europe/Kyiv` нельзя, иначе дата сдвигается. Форматируется как дата UTC.
- **Номера хранятся нормализованными** (без пробелов и точек, в верхнем регистре),
  телефон — в международном виде `+31…`, индекс Нидерландов — `1234 AB`. Нормализация
  выполняется в схеме Zod, как у логина пользователей.
- Денежных полей нет.

### 8.4 События аудита этапа 2

| Событие | `action` | `entity` / `entityId` | `changes` |
|---|---|---|---|
| Первое заполнение сведений о компании | `CREATE` | `CompanyProfile` / запись | заполненные поля |
| Изменение сведений о компании | `UPDATE` | `CompanyProfile` / запись | изменённые поля |

- `"CompanyProfile"` добавляется в `AUDIT_ENTITIES` (`src/lib/audit`).
- Адреса, телефоны, социальные сети и виды деятельности отдельных записей журнала
  не получают: всё сохранение формы — одна запись `CompanyProfile`. Поля `changes`:
  `officeAddress`, `postalAddress`, `warehouses`, `phones`, `socialLinks`, `activities` —
  читаемой строкой (`ТЗ.md`, п. 5.8); служебные `version`, `singleton`, `sortOrder`
  и идентификаторы в `changes` не попадают.
- `summary`: «Заполнены сведения о компании» / «Изменены сведения о компании».

### 8.5 Диаграмма связей этапа 2

```
CompanyProfile ──< CompanyAddress     офис, почтовый, склады (мягкое удаление)
CompanyProfile ──< CompanyPhone       дополнительные телефоны
CompanyProfile ──< CompanySocialLink  социальные сети
CompanyProfile ──< CompanyActivity    виды деятельности SBI
User ──< CompanyProfile, CompanyAddress   createdBy / updatedBy (SetNull)
```

## 9. Этап 3: справочники и проекты

Требования — `ТЗ.md`, раздел 6.

### 9.1 Перечисления

```prisma
enum CustomerType {
  COMPANY          // компания
  PERSON           // частное лицо
}

enum ProjectStatus {
  IN_PROGRESS      // в работе
  CLOSED           // закрыт: изменения запрещены
}

enum VatRate {
  STANDARD_21      // 21 %
  REDUCED_9        // 9 %
  ZERO             // 0 %
  REVERSE_CHARGE   // verlegd, обратное начисление
}
```

`LegalForm` из раздела 8.1 используется и у подрядчика.

### 9.2 Модели

```prisma
model Customer {
  id                  String        @id @default(cuid())
  type                CustomerType  @default(COMPANY)
  name                String                               // название компании или ФИО
  kvkNumber           String?       @unique                // только COMPANY
  vatId               String?                              // только COMPANY
  contactPerson       String?                              // только COMPANY
  email               String?
  phone               String?

  street              String?
  houseNumber         Int?
  houseNumberAddition String?
  postcode            String?
  city                String?
  country             String?       @db.Char(2)            // заполнен вместе с адресом

  comment             String?
  isActive            Boolean       @default(true)         // false — в архиве

  createdAt           DateTime      @default(now())
  updatedAt           DateTime      @updatedAt
  createdById         String?
  updatedById         String?

  createdBy           User?         @relation("CustomerCreatedBy", fields: [createdById], references: [id], onDelete: SetNull)
  updatedBy           User?         @relation("CustomerUpdatedBy", fields: [updatedById], references: [id], onDelete: SetNull)
  projects            Project[]

  @@index([name])
  @@index([isActive])
}

model Contractor {
  id                  String        @id @default(cuid())
  name                String
  legalForm           LegalForm?
  kvkNumber           String?       @unique
  vatId               String?
  contactPerson       String?
  email               String?
  phone               String?

  street              String?
  houseNumber         Int?
  houseNumberAddition String?
  postcode            String?
  city                String?
  country             String?       @db.Char(2)

  comment             String?
  isActive            Boolean       @default(true)

  createdAt           DateTime      @default(now())
  updatedAt           DateTime      @updatedAt
  createdById         String?
  updatedById         String?

  createdBy           User?         @relation("ContractorCreatedBy", fields: [createdById], references: [id], onDelete: SetNull)
  updatedBy           User?         @relation("ContractorUpdatedBy", fields: [updatedById], references: [id], onDelete: SetNull)
  users               User[]        @relation("UserContractor")

  @@index([name])
  @@index([isActive])
}

model Project {
  id                  String        @id @default(cuid())
  number              String                               // уникален среди неудалённых, см. 9.3
  name                String
  customerId          String

  street              String                               // адрес объекта
  houseNumber         Int
  houseNumberAddition String?
  postcode            String
  city                String
  country             String        @default("NL") @db.Char(2)

  startDate           DateTime      @db.Date
  description         String?

  budgetAmount        Decimal?      @db.Decimal(14, 2)     // без НДС, EUR
  vatRate             VatRate?
  budgetHours         Decimal?      @db.Decimal(10, 2)

  status              ProjectStatus @default(IN_PROGRESS)
  closedAt            DateTime?
  closedById          String?

  createdAt           DateTime      @default(now())
  updatedAt           DateTime      @updatedAt
  createdById         String?
  updatedById         String?
  deletedAt           DateTime?

  customer            Customer      @relation(fields: [customerId], references: [id], onDelete: Restrict)
  closedBy            User?         @relation("ProjectClosedBy", fields: [closedById], references: [id], onDelete: SetNull)
  createdBy           User?         @relation("ProjectCreatedBy", fields: [createdById], references: [id], onDelete: SetNull)
  updatedBy           User?         @relation("ProjectUpdatedBy", fields: [updatedById], references: [id], onDelete: SetNull)

  @@index([status, startDate])
  @@index([customerId])
  @@index([name])
}
```

У `User` появляются поле `contractorId String?`, связь
`contractor Contractor? @relation("UserContractor", fields: [contractorId], references: [id], onDelete: Restrict)`,
индекс `@@index([contractorId])` и обратные связи `CustomerCreatedBy`, `CustomerUpdatedBy`,
`ContractorCreatedBy`, `ContractorUpdatedBy`, `ProjectClosedBy`, `ProjectCreatedBy`,
`ProjectUpdatedBy`.

### 9.3 Пояснения к решениям

- **Заказчики и подрядчики не удаляются, а архивируются** (`isActive`), как пользователи,
  поэтому `deletedAt` у них нет — это исключение из правила 3 раздела 7. На них ссылаются
  проекты, журнал и (у подрядчика) учётные записи; архивная запись остаётся видимой в старых
  проектах, но не предлагается в формах. Связи на них — `Restrict`: запись, на которую
  ссылаются, нельзя удалить и в обход приложения.
- **Поля по виду заказчика.** У частного лица `kvkNumber`, `vatId` и `contactPerson` всегда
  `NULL`: схема Zod обнуляет их при `type = PERSON`, а ограничение
  `CHECK ("type" = 'COMPANY' OR ("kvkNumber" IS NULL AND "vatId" IS NULL AND "contactPerson" IS NULL))`
  дописывается в SQL миграции.
- **`kvkNumber` уникален** обычным индексом: в PostgreSQL несколько `NULL` уникальность
  не нарушают. Номер хранится нормализованным (раздел 8.3), поэтому учитывать регистр
  при сравнении не нужно. Уникальность у заказчиков и у подрядчиков раздельная: одна
  организация может быть и тем, и другим.
- **Адрес колонками в записи**, а не ссылкой на `CompanyAddress`: адрес заказчика, подрядчика
  и объекта принадлежит только своей записи и меняется вместе с ней. Правила полей те же,
  что у адреса офиса (`ТЗ.md`, п. 5.4). У справочников адрес необязателен, и `country`
  заполнен, только когда заполнен адрес; у проекта адрес обязателен.
- **Номер проекта** уникален среди неудалённых проектов без учёта регистра — частичным
  уникальным индексом, дописанным в SQL миграции:
  `CREATE UNIQUE INDEX "Project_number_active_key" ON "Project" (lower("number")) WHERE "deletedAt" IS NULL`.
  Удалённый проект освобождает номер. Нарушение индекса при сохранении превращается
  в ошибку поля «Проект с таким номером уже есть».
- **Предлагаемый номер** вычисляется выборкой наибольшего числового хвоста среди номеров
  вида `ГГГГ-число` текущего года, включая удалённые проекты, и не резервируется
  (`ТЗ.md`, п. 6.6). Отдельной таблицы-счётчика нет: номер можно править вручную, поэтому
  счётчик всё равно не гарантировал бы, что номер свободен.
- **Бюджет:** хранятся `budgetAmount` без НДС и `vatRate`; сумма с НДС вычисляется при
  показе и выгрузке и не хранится — иначе её пришлось бы согласовывать при каждой правке.
  Ограничение `CHECK ("budgetAmount" IS NULL OR "vatRate" IS NOT NULL)` дописывается в SQL
  миграции. Часы — `Decimal`, а не `Float`, по той же причине, что и деньги.
- **Статус и закрытие:** `closedAt` заполнен ровно у закрытого проекта —
  `CHECK (("status" = 'CLOSED') = ("closedAt" IS NOT NULL))` в SQL миграции. Запрет изменений
  закрытого проекта выполняется условием записи
  `updateMany({ where: { id, status: "IN_PROGRESS", deletedAt: null } })`; ноль обновлённых
  строк — ошибка «Проект закрыт». Проверки статуса перед записью недостаточно: проект могут
  закрыть между чтением и записью. Этапы 4 и 5 проверяют статус проекта в транзакции записи
  отчёта или расхода — блокировкой строки проекта (раздел 10.6).
- **Проверка ограничений, дописанных вручную.** Все `CHECK` и частичные индексы разделов 8
  и 9 проверяет `tests/db/manual-constraints.test.ts`: запись-нарушение должна отклоняться
  ограничением с этим именем, допустимая запись — проходить, а список `CHECK` и частичных
  индексов базы должен совпадать с перечнем в тесте. Новое ограничение, дописанное в SQL
  миграции, добавляется туда же вместе с его проверками.
- **`startDate` — только дата** (`@db.Date`), как `registeredOn` в разделе 8.3: форматируется
  как дата UTC. Длительность не хранится: она вычисляется от `startDate` до сегодняшней даты
  по `Europe/Kyiv` или до даты `closedAt` по `Europe/Kyiv`.
- **Удаление проекта мягкое** (`deletedAt`), все выборки проектов исключают удалённые.
  Закрытый проект не удаляется: условие `status = IN_PROGRESS` входит в запись удаления.
- **Привязка подрядчика** `User.contractorId` заполняется только у роли `CONTRACTOR`; при смене
  роли действие обнуляет поле в той же транзакции. Ограничение в базе не вводится: роль
  и привязка меняются одной формой, правило проверяют схема Zod и действие.

### 9.4 События аудита этапа 3

| Событие | `action` | `entity` / `entityId` | `changes` |
|---|---|---|---|
| Создание заказчика, подрядчика, проекта | `CREATE` | `Customer`, `Contractor`, `Project` / запись | заполненные поля |
| Изменение заказчика, подрядчика, проекта | `UPDATE` | то же | изменённые поля, кроме статуса |
| Архив и восстановление заказчика, подрядчика | `STATUS_CHANGE` | `Customer`, `Contractor` / запись | `isActive`: «Активен» / «В архиве» |
| Закрытие проекта, возврат в работу | `STATUS_CHANGE` | `Project` / проект | `status`: «В работе» / «Закрыт» |
| Удаление проекта | `DELETE` | `Project` / проект | — |
| Привязка учётной записи к подрядчику | `UPDATE` | `User` / пользователь | `contractor`: название подрядчика |
| Выгрузка реестра | `EXPORT` | `Customer`, `Contractor`, `Project` / — | — |

- `"Customer"`, `"Contractor"`, `"Project"` добавляются в `AUDIT_ENTITIES` с названиями
  «Заказчик», «Подрядчик», «Проект».
- Адрес пишется одним полем `address` одной строкой, заказчик проекта — полем `customer`
  с названием, ставка НДС — названием, суммы и часы — строкой в формате чисел приложения,
  `startDate` — `dd.MM.yyyy`. `closedAt`, `closedById`, `deletedAt` и идентификаторы в `changes`
  не попадают.
- **Бюджет в журнале.** Поля `budgetAmount`, `vatRate`, `budgetHours` пишутся в `changes`
  как обычно, но выборка истории и журнала для пользователя без `projects.budget.read`
  заменяет их одной строкой «Бюджет: изменён» без значений (`ТЗ.md`, п. 6.10). Список полей
  бюджета задаётся явной константой рядом с выборкой журнала — так же, как список
  исключаемых полей из раздела 4.1. На этапе 4 константа заменяется описанием полей
  сущности (раздел 10.8).
- `summary` называет запись номером и названием проекта или названием заказчика и подрядчика
  и не содержит сумм.

### 9.5 Диаграмма связей этапа 3

```
Customer ──< Project            заказчик проекта (Restrict)
Contractor ──< User             учётные записи подрядчика (Restrict)
User ──< Project                closedBy, createdBy / updatedBy (SetNull)
User ──< Customer, Contractor   createdBy / updatedBy (SetNull)
```

## 10. Этап 4: отчёты о работе

Требования — `ТЗ.md`, раздел 7.

### 10.1 Перечисления

```prisma
enum WorkReportStatus {
  NOT_APPROVED     // не утверждён
  APPROVED         // утверждён
}

enum ImportKind {
  WORK_REPORTS     // отчёты о работе; этап 6 добавит свои виды
}

enum ImportBatchStatus {
  PREVIEW          // файл разобран, показан предпросмотр, в базу ничего не записано
  COMMITTED        // импорт подтверждён и записан
}
```

### 10.2 Изменения пользователя

```prisma
model User {
  // ...поля этапов 1–3, кроме прежнего смысла fullName
  lastName           String
  firstName          String?                // NULL только у записей до этапа 4, см. 10.6
  fullName           String                 // «Фамилия Имя», заполняет схема Zod, см. 10.6
  nickname           String?                // только EMPLOYEE и CONTRACTOR, см. 10.6–10.7

  workReports        WorkReport[] @relation("WorkReportWorker")
  // обратные связи: WorkReportApprovedBy, WorkReportUnapprovedBy, WorkReportCreatedBy,
  // WorkReportUpdatedBy, ImportBatchCreatedBy, ImportBatchUpdatedBy
}
```

`@@index([fullName])` остаётся: поиск и сортировка по ФИО работают по-прежнему.

### 10.3 Отчёт о работе

```prisma
model WorkReport {
  id                String           @id @default(cuid())
  workerId          String                                  // на кого записан отчёт
  contractorId      String?                                 // организация работника на момент создания;
                                                            //   NULL — сотрудник компании
  projectId         String

  date              DateTime         @db.Date
  startMinutes      Int                                     // минуты от полуночи, кратно 15
  endMinutes        Int                                     // до 1440 (24:00)
  lunchMinutes      Int              @default(0)
  workedMinutes     Int                                     // end − start − lunch, см. 10.6
  mileageKm         Int              @default(0)
  workDescription   String                                  // выполненные работы

  status            WorkReportStatus @default(NOT_APPROVED)
  approvedAt        DateTime?
  approvedById      String?
  unapprovalReason  String?                                 // причина последнего снятия
  unapprovedAt      DateTime?
  unapprovedById    String?

  importBatchId     String?                                 // импорт, которым создан отчёт

  createdAt         DateTime         @default(now())
  updatedAt         DateTime         @updatedAt
  createdById       String?
  updatedById       String?
  deletedAt         DateTime?

  worker            User             @relation("WorkReportWorker", fields: [workerId], references: [id], onDelete: Restrict)
  contractor        Contractor?      @relation(fields: [contractorId], references: [id], onDelete: Restrict)
  project           Project          @relation(fields: [projectId], references: [id], onDelete: Restrict)
  importBatch       ImportBatch?     @relation(fields: [importBatchId], references: [id], onDelete: Restrict)
  approvedBy        User?            @relation("WorkReportApprovedBy", fields: [approvedById], references: [id], onDelete: SetNull)
  unapprovedBy      User?            @relation("WorkReportUnapprovedBy", fields: [unapprovedById], references: [id], onDelete: SetNull)
  createdBy         User?            @relation("WorkReportCreatedBy", fields: [createdById], references: [id], onDelete: SetNull)
  updatedBy         User?            @relation("WorkReportUpdatedBy", fields: [updatedById], references: [id], onDelete: SetNull)

  @@index([workerId, date])
  @@index([projectId, date])
  @@index([contractorId, date])
  @@index([status, date])
}
```

У `Project` и `Contractor` появляется обратная связь `workReports WorkReport[]`.

### 10.4 Импорт

```prisma
model ImportBatch {
  id              String            @id @default(cuid())
  kind            ImportKind
  status          ImportBatchStatus @default(PREVIEW)
  fileName        String
  fileSize        Int                                       // байты
  rowCount        Int                                       // строк данных в файле
  errorCount      Int                                       // строк с ошибками при последней проверке
  rows            Json                                      // разобранные строки с ошибками, см. 10.6
  approveOnImport Boolean?                                  // выбор «Сразу утвердить» при подтверждении
  expiresAt       DateTime                                  // предпросмотр действителен 24 часа
  committedAt     DateTime?

  createdAt       DateTime          @default(now())
  updatedAt       DateTime          @updatedAt
  createdById     String?
  updatedById     String?

  createdBy       User?             @relation("ImportBatchCreatedBy", fields: [createdById], references: [id], onDelete: SetNull)
  updatedBy       User?             @relation("ImportBatchUpdatedBy", fields: [updatedById], references: [id], onDelete: SetNull)
  workReports     WorkReport[]

  @@index([createdById, status])
  @@index([status, expiresAt])
}
```

### 10.5 Миграции этапа

1. `user_names_nickname` — `lastName`, `firstName`, `nickname`; перенос прежнего `fullName`
   целиком в `lastName` (10.6); ограничения пользователя из 10.7.
2. `work_reports` — перечисления, `WorkReport`, `ImportBatch`, расширение `btree_gist`,
   ограничения отчёта и частичный уникальный индекс названия проекта из 10.7.

Обе создаются с `--create-only`, ручной SQL дописывается до применения. После применения
повторный `npx prisma migrate dev` должен сообщать, что схема синхронна: если Prisma предлагает
удалить ручное ограничение или индекс, работа останавливается и решение обсуждается (для
пересечений запасной путь — `pg_advisory_xact_lock` по работнику и дате, 10.6).

### 10.6 Пояснения к решениям

- **Время — минутами от полуночи** (`Int`), дата — `@db.Date`. Это местное время на объекте,
  как его записал работник (`ТЗ.md`, п. 7.4): в UTC оно не переводится, иначе смена часового
  пояса или летнего времени сдвинула бы отчёт. «Сегодня» для проверки даты — по `Europe/Kyiv`,
  как у остальных дат приложения. `date` форматируется как дата UTC, как `startDate` проекта.
- **Шаг 15 минут** закреплён и в схеме Zod, и `CHECK` в базе. Десятичные часы — `workedMinutes / 60`
  — при таком шаге всегда точны до сотых (15 минут = 0,25 ч): перевод выполняется одной функцией
  в целых сотых (`workedMinutes / 15 × 25`), без арифметики с плавающей точкой, а суммы часов
  считаются суммой минут. Поэтому отдельного `Decimal` для часов отчёта нет.
- **`workedMinutes` хранится**, хотя вычисляется: итоги проекта, участников и табеля — это
  `SUM("workedMinutes")` в базе (`aggregate`/`groupBy` Prisma), а не выражение над тремя колонками.
  Согласованность закреплена `CHECK` (10.7), значение вычисляет схема Zod.
- **Пробег — целые километры** (`Int`, 0–2 000), пустое поле формы — 0 (`ТЗ.md`, п. 7.4).
- **Организация — снимок.** `contractorId` заполняется при создании отчёта из `User.contractorId`
  работника и при смене работника в отчёте; изменение привязки работника прежние отчёты не
  трогает. `NULL` — сотрудник компании («Сотрудники компании» в фильтрах и участниках).
- **Допустимость работника** (активен, роль `EMPLOYEE` или `CONTRACTOR`, у подрядчика — активная
  организация) проверяет действие в транзакции записи; ограничением базы это не выражается.
- **Утверждение.** `approvedAt` / `approvedById` заполнены ровно у утверждённого отчёта; причина
  снятия хранится вместе с моментом и автором снятия и только у неутверждённого отчёта (`CHECK`
  в 10.7). Утверждение очищает поля снятия, снятие — поля утверждения; прежние утверждения
  и причины остаются в журнале. Переходы статуса — условием записи:
  `updateMany({ where: { id, status: "NOT_APPROVED", deletedAt: null } })` для утверждения
  и наоборот для снятия; ноль строк — «Статус отчёта уже изменён». Правка работником — с условием
  `status = NOT_APPROVED`, ноль строк — «Отчёт уже утверждён».
- **Удаление отчёта мягкое** (`deletedAt`): удалённый отчёт не участвует ни в выборках, ни
  в итогах, ни в проверке пересечений.
- **Пересечения по времени** исключает ограничение `EXCLUDE` (10.7) по работнику, дате
  и промежутку `int4range(startMinutes, endMinutes)` — полуоткрытому, поэтому отчёты встык
  не пересекаются. Для равенства текстового `workerId` в индексе GiST нужно расширение
  `btree_gist` (стандартное расширение PostgreSQL, есть в образе `postgres:16`). Действие сначала
  ищет пересекающийся отчёт обычной выборкой, чтобы назвать его в ошибке; ограничение ловит
  одновременные сохранения, и его нарушение (`SQLSTATE 23P01`) превращается в ту же ошибку поля.
- **Закрытый проект и отчёты.** Каждая транзакция, меняющая отчёт (ввод, правка, удаление,
  утверждение, снятие, импорт), первым делом блокирует строку проекта:
  `SELECT 1 FROM "Project" WHERE id = $1 AND status = 'IN_PROGRESS' AND "deletedAt" IS NULL FOR SHARE`
  (при переносе отчёта — оба проекта, в порядке `id`, чтобы не было взаимной блокировки). Нет
  строки — «Проект закрыт». Закрытие проекта, его удаление и правка даты начала сначала меняют
  строку проекта (`UPDATE` берёт на неё блокировку), затем в той же транзакции считают отчёты:
  неутверждённые — для закрытия, любые — для удаления, самую раннюю дату — для даты начала;
  при нарушении транзакция откатывается. Запись отчёта, начатая раньше, завершается до `UPDATE`
  проекта и попадает в подсчёт; начатая позже ждёт конца транзакции проекта и в режиме
  `READ COMMITTED` видит уже закрытый проект. Условие `updateMany` на статусе проекта (9.3)
  здесь не годится: отчёт — другая таблица.
- **Название проекта «В работе»** уникально частичным индексом по `lower("name")` (10.7).
  Нарушение при создании и правке превращается в ошибку поля `name`, при возврате в работу —
  в ошибку действия, называющую проект с тем же названием. Перед миграцией `work_reports`
  проверяется, нет ли в базе разработчика двух неудалённых проектов «В работе» с одинаковым
  названием: если есть — миграция не применяется, пока пользователь не решит, какой переименовать.
- **ФИО из фамилии и имени.** `fullName` остаётся колонкой: на ней построены поиск, сортировка,
  выгрузка и отметки «кем изменено» этапов 1–3. Её заполняет схема Zod из `lastName` и `firstName`
  («Фамилия Имя»), в форме её нет, а `CHECK` (10.7) не даёт ей разойтись с полями. Фамилия и имя
  вводятся вручную и из строки не выводятся (`ТЗ.md`, п. 7.11).
- **Перенос существующих записей.** Миграция не делит прежнее `fullName`: оно целиком
  становится `lastName`, `firstName` остаётся `NULL`, `fullName` не меняется. Поэтому
  `firstName` в базе необязателен (`String?`), а `CHECK` допускает оба случая: при пустом имени
  `fullName` равен фамилии. `NULL` возможен только у записей до этапа 4 — схема Zod требует имя
  при каждом создании и сохранении, так что после правки записи он исчезает; по `firstName IS NULL`
  реестр находит записи «Имя не заполнено».
- **Никнейм** хранится как введён (после нормализации пробелов схемой Zod), уникальность — без
  учёта регистра индексом по `lower("nickname")` (10.7); несколько `NULL` индекс не нарушают.
  Никнейм есть только у ролей `EMPLOYEE` и `CONTRACTOR` (`CHECK`), но может быть пустым у
  записей до этапа 4: миграция никнеймы не заполняет, имени для предложения у них нет.
  Обязательность для новых записей и правок обеспечивает схема Zod. Смена роли на `ADMIN` или
  `MANAGER` обнуляет никнейм в той же транзакции, как `contractorId`.
- **Импорт хранит разобранный файл, а не сам файл.** При загрузке файл читается в памяти
  (ExcelJS), строки нормализуются и проверяются, результат — значения и ошибки каждой строки —
  пишется в `ImportBatch.rows` (`jsonb`); сам файл не сохраняется (хранение файлов — этап 5).
  Предпросмотр листает `rows`, подтверждение проверяет строки заново и создаёт отчёты в одной
  транзакции, переводя пакет в `COMMITTED` условием `status = PREVIEW` — повторное подтверждение
  ничего не создаёт. Пакеты `PREVIEW` с истёкшим `expiresAt` удаляются жёстко при следующей
  загрузке: это черновик, на который ничто не ссылается и которого нет в журнале, — исключение
  из правила 3 раздела 7. Подтверждённые пакеты не удаляются: на них ссылаются отчёты и журнал.

### 10.7 Ограничения, дописываемые в SQL миграции вручную

| Имя | Миграция | SQL | Что гарантирует |
|---|---|---|---|
| `User_full_name_check` | `user_names_nickname` | `CHECK ("fullName" = CASE WHEN "firstName" IS NULL THEN "lastName" ELSE "lastName" \|\| ' ' \|\| "firstName" END)` | ФИО не расходится с фамилией и именем, в том числе у записей без имени |
| `User_nickname_role_check` | `user_names_nickname` | `CHECK ("nickname" IS NULL OR "role" IN ('EMPLOYEE', 'CONTRACTOR'))` | Никнейм только у работников |
| `User_nickname_lower_key` | `user_names_nickname` | `CREATE UNIQUE INDEX … ON "User" (lower("nickname"))` | Никнейм уникален без учёта регистра |
| `btree_gist` | `work_reports` | `CREATE EXTENSION IF NOT EXISTS btree_gist` | Равенство `workerId` в ограничении `EXCLUDE` |
| `Project_name_in_progress_key` | `work_reports` | `CREATE UNIQUE INDEX … ON "Project" (lower("name")) WHERE "status" = 'IN_PROGRESS' AND "deletedAt" IS NULL` | Название проекта «В работе» уникально |
| `WorkReport_time_check` | `work_reports` | `CHECK ("startMinutes" >= 0 AND "startMinutes" < "endMinutes" AND "endMinutes" <= 1440 AND "lunchMinutes" >= 0 AND "startMinutes" % 15 = 0 AND "endMinutes" % 15 = 0 AND "lunchMinutes" % 15 = 0)` | Время в пределах суток, шаг 15 минут |
| `WorkReport_worked_minutes_check` | `work_reports` | `CHECK ("workedMinutes" = "endMinutes" - "startMinutes" - "lunchMinutes" AND "workedMinutes" > 0)` | Отработано вычислено верно, обед меньше рабочего времени |
| `WorkReport_mileage_check` | `work_reports` | `CHECK ("mileageKm" BETWEEN 0 AND 2000)` | Пробег в допустимых пределах |
| `WorkReport_approval_check` | `work_reports` | `CHECK (("status" = 'APPROVED') = ("approvedAt" IS NOT NULL))` | Отметка утверждения ровно у утверждённого |
| `WorkReport_unapproval_check` | `work_reports` | `CHECK (("unapprovalReason" IS NULL) = ("unapprovedAt" IS NULL) AND ("unapprovalReason" IS NULL OR "status" = 'NOT_APPROVED'))` | Причина снятия — только у неутверждённого, вместе с моментом снятия |
| `WorkReport_no_overlap` | `work_reports` | `EXCLUDE USING gist ("workerId" WITH =, "date" WITH =, int4range("startMinutes", "endMinutes") WITH &&) WHERE ("deletedAt" IS NULL)` | Отчёты работника за день не пересекаются |
| `ImportBatch_committed_check` | `work_reports` | `CHECK (("status" = 'COMMITTED') = ("committedAt" IS NOT NULL))` | Момент подтверждения ровно у подтверждённого пакета |

**Все они добавляются в `tests/db/manual-constraints.test.ts`** (раздел 9.3): на каждое —
запись-нарушение, отклоняемая именно этим ограничением, и допустимая запись; для пересечений —
пересекающийся отчёт отклоняется, отчёт встык, отчёт другого работника или дня и отчёт поверх
удалённого проходят; для названия и никнейма — другой регистр отклоняется, закрытый и удалённый
проект не мешают. Сверка полного списка в том же тесте расширяется: к `CHECK` и частичным
и выражениям-индексам добавляются ограничения `EXCLUDE` (`contype = 'x'`) и список расширений
базы (`pg_extension`) — потеря `btree_gist` тоже должна ронять `npm test`.

### 10.8 Описание полей сущности для журнала

Явная константа `PROJECT_BUDGET_FIELDS` (9.4) заменяется описанием полей сущностей журнала
в `src/lib/audit` (решение 18.09.2026): для каждой сущности перечислены поля, значения которых
видны не всем, — с правом на просмотр и группой, которой они заменяются в истории
(`Project`: `budgetAmount`, `vatRate`, `budgetHours` → право `projects.budget.read`, группа
«Бюджет»), — и поля `Decimal`, открытые всем, помеченные явно. Выборка истории и журнала
заменяет недоступные поля одной строкой группы «Бюджет: изменён», как раньше, но по описанию,
а не по имени сущности в коде.

Тест разбирает схему Prisma и падает, если у модели, входящей в `AUDIT_ENTITIES`, есть поле
`Decimal` без описания — ни «ограничено правом», ни «открыто». Новое денежное поле этапа 5 не
сможет попасть в историю сотрудника незамеченным. У отчёта о работе денежных полей нет.

### 10.9 События аудита этапа 4

| Событие | `action` | `entity` / `entityId` | `changes` |
|---|---|---|---|
| Создание отчёта (формой или импортом) | `CREATE` | `WorkReport` / отчёт | заполненные поля |
| Изменение отчёта | `UPDATE` | `WorkReport` / отчёт | изменённые поля |
| Удаление отчёта | `DELETE` | `WorkReport` / отчёт | — |
| Утверждение, снятие утверждения | `STATUS_CHANGE` | `WorkReport` / отчёт | `status`; при снятии — `unapprovalReason` |
| Подтверждённый импорт | `IMPORT` | `ImportBatch` / пакет | — |
| Выгрузка отчётов и табеля | `EXPORT` | `WorkReport` / — | — |
| Фамилия, имя, никнейм | `UPDATE` | `User` / пользователь | `lastName`, `firstName`, `nickname` |

- `"WorkReport"` и `"ImportBatch"` добавляются в `AUDIT_ENTITIES` с названиями «Отчёт о работе»
  и «Импорт»; `HISTORY_PERMISSION`: `WorkReport` — `workReports.history`, `ImportBatch` —
  `audit.read`.
- Поля `changes` отчёта: `worker` (ФИО), `organization` (название или «Сотрудники компании»),
  `project` («2026-001 «Название»»), `date`, `startTime`, `endTime`, `lunch`, `worked`, `mileageKm`,
  `workDescription`, `status`. Минуты пишутся читаемым временем (`07:00`, `0:30`), а не числом.
  Служебные `approvedAt`, `approvedById`, `unapprovedAt`, `importBatchId`, `deletedAt`
  и идентификаторы в `changes` не попадают; кто и когда утвердил — это автор и время записи
  `STATUS_CHANGE`.
- Импорт пишет одну запись `IMPORT` и по записи `CREATE` на каждый созданный отчёт — все в
  транзакции подтверждения; история каждого отчёта начинается с его создания.
- `fullName` в `changes` пользователя не пишется: он производный от `lastName` и `firstName`.
- `summary` — по `ТЗ.md`, п. 7.14.

### 10.10 Диаграмма связей этапа 4

```
User ──< WorkReport          работник отчёта (Restrict), approvedBy / unapprovedBy,
                             createdBy / updatedBy (SetNull)
Project ──< WorkReport       объект отчёта (Restrict)
Contractor ──< WorkReport    организация работника на момент отчёта (Restrict)
ImportBatch ──< WorkReport   импорт, которым создан отчёт (Restrict)
User ──< ImportBatch         createdBy / updatedBy (SetNull)
```
