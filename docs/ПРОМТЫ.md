# Промты для поэтапной разработки

Готовые задания для Claude Cowork. Каждый шаг — отдельная задача с проверяемым
результатом.

## Как этим пользоваться

1. **Один шаг — одна сессия.** Не склеивайте шаги: длинная сессия теряет
   точность, а проверять результат становится нечем.
2. **Копируйте промт целиком**, вместе с разделами «Критерии приёмки» и
   «Чего не делать» — именно они удерживают работу в границах шага.
3. **Проверяйте критерии приёмки сами**, руками в браузере, прежде чем переходить
   к следующему шагу. Пропущенная на шаге 2 ошибка на шаге 6 обойдётся дороже.
4. **Не переходите к следующему шагу, пока предыдущий не принят.** Каждый шаг
   опирается на предыдущий.
5. Если Claude предлагает решение, расходящееся с `docs/ТЗ.md`, — сначала
   обсудите расхождение и при необходимости поправьте ТЗ, а не код. Документ
   должен оставаться источником истины.

---

## Шаг 0. Каркас проекта

```
Мы начинаем разработку приложения ConstructionProjects. Прочитай docs/ТЗ.md
и docs/АРХИТЕКТУРА.md — это источник истины по требованиям и стеку.

ЦЕЛЬ
Подготовить каркас проекта, который запускается и подключается к базе данных.
Функциональности приложения на этом шаге нет.

ЧТО СДЕЛАТЬ
1. Инициализируй проект Next.js 15 с App Router, TypeScript в режиме strict,
   Tailwind CSS, каталогом src и псевдонимом путей "@/*".
2. Настрой ESLint и Prettier, добавь скрипты lint и typecheck.
3. Подключи shadcn/ui и добавь базовые компоненты: button, input, label, table,
   dialog, select, checkbox, badge, dropdown-menu, sonner, form.
4. Подключи Prisma с провайдером PostgreSQL. Создай пустую схему без моделей.
5. Создай docker-compose.yml с PostgreSQL 16, томом для данных и портом 5432.
6. Создай .env.example со всеми нужными переменными: DATABASE_URL,
   SEED_ADMIN_PASSWORD, POSTGRES_PASSWORD. Настоящий .env добавь в .gitignore.
7. Подключи next-intl с единственной локалью ru и файлом messages/ru.json.
   Не используй маршруты вида /ru/... — локаль пока одна и берётся из настроек.
8. Подключи Vitest и Playwright с минимальной конфигурацией и по одному
   демонстрационному тесту.
9. Создай src/lib/db.ts с клиентом Prisma, устойчивым к горячей перезагрузке
   в режиме разработки.
10. Создай src/lib/format.ts с функциями форматирования даты (dd.MM.yyyy),
    даты со временем (dd.MM.yyyy HH:mm) и числа (пробел между разрядами,
    запятая как десятичный разделитель), с таймзоной Europe/Kyiv.
11. Напиши README.md с инструкцией локального запуска.

КРИТЕРИИ ПРИЁМКИ
- docker compose up -d db поднимает базу, npx prisma db push выполняется без ошибок.
- npm run dev открывает страницу на http://localhost:3000.
- npm run lint, npm run typecheck и npm test проходят без ошибок.
- В .env.example перечислены все переменные; настоящий .env не попадает в git.

ЧЕГО НЕ ДЕЛАТЬ
- Не создавай модели базы данных, страницы входа и разделы приложения —
  это следующие шаги.
- Не добавляй библиотек сверх перечисленных в docs/АРХИТЕКТУРА.md.
```

---

## Шаг 1. Схема базы данных

```
ЦЕЛЬ
Создать модель данных этапа 1 и начальные данные.

ИСТОЧНИК
docs/СХЕМА-БД.md — там описаны все модели, поля, индексы и пояснения к решениям.
Следуй документу точно; при расхождении сначала спроси.

ЧТО СДЕЛАТЬ
1. Опиши в prisma/schema.prisma перечисления Role и AuditAction и модели
   User, Session, AuditLog ровно так, как задано в docs/СХЕМА-БД.md,
   вместе со всеми индексами и правилами удаления связей.
2. Создай первую миграцию: npx prisma migrate dev --name init
3. Напиши prisma/seed.ts: создаёт администратора с логином admin, ФИО
   "Администратор системы", ролью ADMIN и паролем из переменной окружения
   SEED_ADMIN_PASSWORD.
   Если переменная не задана — завершиться с понятной ошибкой.
   Повторный запуск не должен создавать дубликат.
4. Подключи @node-rs/argon2 и вынеси хэширование и проверку пароля
   в src/lib/auth/password.ts (алгоритм Argon2id).
5. Добавь скрипты db:seed и db:studio в package.json.

КРИТЕРИИ ПРИЁМКИ
- npx prisma migrate dev проходит на чистой базе без ручных правок.
- npm run db:seed создаёт администратора; повторный запуск не создаёт второго.
- Запуск сида без SEED_ADMIN_PASSWORD завершается ошибкой, а не паролем
  по умолчанию.
- npx prisma studio показывает три таблицы с ожидаемыми полями.

ЧЕГО НЕ ДЕЛАТЬ
- Не добавляй моделей будущих этапов (проекты, сметы, справочники).
- Не храни пароль администратора в коде или в миграции.
```

---

## Шаг 2. Аутентификация

```
ЦЕЛЬ
Реализовать вход, выход и защиту маршрутов.

ИСТОЧНИК
docs/ТЗ.md, разделы 3.3 и 4.3. docs/АРХИТЕКТУРА.md, раздел 4 — там обосновано,
почему используются сессии в базе, а не JWT.

ЧТО СДЕЛАТЬ
1. src/lib/auth/session.ts:
   - createSession(userId, ip, userAgent): генерирует случайный токен на 32 байта,
     сохраняет в базе его хэш SHA-256, возвращает сам токен.
   - validateSession(token): возвращает пользователя, если сессия не отозвана и
     не истекла; продлевает сессию, если последняя активность была больше часа назад.
   - invalidateSession(sessionId) и invalidateAllUserSessions(userId).
   - Cookie session: httpOnly, SameSite=Lax, Secure в продакшне, срок 12 часов.
2. src/lib/auth/current-user.ts: getCurrentUser() и requireUser() —
   requireUser перенаправляет на /login, если сессии нет.
3. Страница /login с формой (логин, пароль, показать пароль) и server action
   входа по правилам ТЗ п. 4.3:
   - логин нормализуется в нижний регистр;
   - при неверных данных или деактивации администратором — единый текст
     «Неверный логин или пароль»;
   - после 5 неудач подряд учётная запись блокируется на 15 минут, показывается
     «Вход временно заблокирован. Попробуйте через N мин.»;
   - при успехе счётчик неудач обнуляется, обновляется lastLoginAt.
4. Выход: server action, отзывает сессию и удаляет cookie.
5. src/middleware.ts: неавторизованный запрос к защищённым маршрутам
   перенаправляется на /login. Middleware проверяет только наличие cookie
   и не обращается к базе — сама сессия проверяется в requireUser().
6. Все тексты — в messages/ru.json.

КРИТЕРИИ ПРИЁМКИ
- Переход на / без авторизации ведёт на /login.
- Вход администратором из сида работает и открывает главную.
- Неверный пароль и несуществующий логин дают один и тот же текст ошибки.
- Шестая подряд неудачная попытка даёт сообщение о временной блокировке;
  через 15 минут вход снова возможен.
- Выход возвращает на /login, кнопка «назад» в браузере не открывает приложение.
- В базе в таблице Session хранится хэш, а не сам токен.

ЧЕГО НЕ ДЕЛАТЬ
- Не подключай NextAuth, Auth.js, Lucia и другие библиотеки аутентификации:
  выбор в пользу собственных сессий обоснован в docs/АРХИТЕКТУРА.md.
- Не реализуй восстановление пароля по email и смену пароля самим пользователем —
  по ТЗ пароли назначает и сбрасывает только администратор.
- Не пиши пока журнал аудита: он будет на шаге 6.
```

---

## Шаг 3. Каркас интерфейса

```
ЦЕЛЬ
Сделать оболочку приложения: навигацию, шапку, главную страницу.

ЧТО СДЕЛАТЬ
1. Общий layout защищённой зоны src/app/(app)/layout.tsx:
   - боковое меню с разделами «Главная», «Пользователи», «Журнал аудита»;
     пункты, недоступные роли, не отображаются;
   - шапка с названием приложения, ФИО текущего пользователя, его ролью
     и кнопкой выхода;
   - область уведомлений (sonner) для сообщений об успехе и ошибках.
2. Главная страница: приветствие по имени и карточки разделов. Карточки будущих
   разделов («Проекты», «Сметы», «Дашборд») показываются неактивными
   с пометкой «В разработке».
3. Страница «Доступ запрещён» с понятным текстом и ссылкой на главную.
4. Страницы ошибки и состояния загрузки (error.tsx, not-found.tsx, loading.tsx).
5. Компонент-обёртка src/components/data-table на TanStack Table: серверная
   пагинация, сортировка, состояние в параметрах URL, пустое состояние,
   выбор числа строк на страницу (25/50/100). Пока без привязки к данным.
6. Все подписи — в messages/ru.json; сгруппируй ключи по разделам.

КРИТЕРИИ ПРИЁМКИ
- После входа видны сайдбар, шапка с ФИО и ролью, работающая кнопка выхода.
- Интерфейс не «ломается» на ширине 1280, 768 и 360 пикселей; уже 1280 пикселей
  боковое меню показывает только пиктограммы.
- В коде компонентов нет ни одной русской строки — все тексты берутся
  из messages/ru.json.
- Переход по несуществующему адресу показывает страницу «Не найдено».

ЧЕГО НЕ ДЕЛАТЬ
- Не подключай таблицу к реальным данным — это шаг 4.
- Не добавляй переключатель языков: вторая локаль появится на этапе 10,
  сейчас важно лишь, чтобы тексты были вынесены.
```

---

## Шаг 4. Реестр и формы пользователей

```
ЦЕЛЬ
Полноценная работа с пользователями: список, создание, редактирование,
деактивация и активация. Пользователи не удаляются.

ИСТОЧНИК
docs/ТЗ.md, разделы 4.4–4.7. Поля формы и правила валидации — таблица в п. 4.5.

ЧТО СДЕЛАТЬ
1. src/features/users/schemas.ts — схемы Zod для создания, редактирования и
   сброса пароля. Логин нормализуется в нижний регистр прямо в схеме.
   Схемы используются и на клиенте, и на сервере.
2. src/features/users/queries.ts — выборка списка с поиском, фильтрами по роли
   и статусу, сортировкой и серверной пагинацией; выборка одного пользователя.
   По умолчанию фильтр статуса — «Активные».
3. src/features/users/actions.ts — createUser, updateUser, resetPassword,
   toggleStatus. Каждое действие начинается с requireUser()
   (проверка прав будет добавлена на шаге 5).
4. Реестр /users по ТЗ п. 4.4: колонки, поиск с задержкой 300 мс, фильтры,
   сортировка, пагинация, состояние в URL, действия в строке,
   кнопка «Создать пользователя».
5. Формы /users/new и /users/[id]/edit на react-hook-form с теми же схемами Zod.
   Логин доступен для ввода только при создании.
6. Карточка /users/[id] со вкладками «Профиль» и «Сессии» (вкладку «История
   изменений» добавим на шаге 6). На вкладке сессий — список активных сессий
   и кнопки завершения одной и всех сессий.
7. Деактивация: снимает isActive и немедленно отзывает все сессии пользователя,
   перед ней — диалог подтверждения с ФИО. Сброс пароля тоже отзывает все сессии
   пользователя.
8. Удаления нет: пользователь остаётся в базе и в реестре со статусом «Неактивен»,
   логин и email не освобождаются (ТЗ п. 4.7).
9. Правила, которые нужно проверять на сервере в транзакции:
   - нельзя деактивировать или понизить в роли последнего активного
     администратора;
   - нельзя деактивировать самого себя.
10. Занятый логин или email возвращает ошибку на поле, а не общий сбой.

КРИТЕРИИ ПРИЁМКИ
- Список фильтруется, сортируется и листается; после обновления страницы
  состояние восстанавливается из адресной строки.
- Созданный пользователь входит с выданным паролем и сразу попадает на главную.
- После сброса пароля открытая сессия пользователя перестаёт работать,
  а войти можно только с новым паролем.
- Попытка создать пользователя с занятым логином показывает ошибку на поле.
- Логины Ivanov и ivanov считаются одним и тем же — второй создать нельзя.
- Деактивация пользователя завершает его открытую сессию: следующее действие
  в другом браузере приводит его на /login.
- Деактивированный пользователь виден в реестре с фильтром «Неактивные» или «Все»;
  создать учётную запись с его логином или email нельзя.
- Попытка деактивировать последнего администратора или самого себя отклоняется
  с понятным сообщением.

ЧЕГО НЕ ДЕЛАТЬ
- Не реализуй журнал аудита — шаг 6.
- Не делай экспорт — шаг 8.
- Не добавляй массовых операций над выбранными строками: в ТЗ их нет.
```

---

## Шаг 5. Права доступа

```
ЦЕЛЬ
Внедрить ролевую модель на сервере и в интерфейсе.

ИСТОЧНИК
docs/ПРАВА-ДОСТУПА.md — матрица прав и правила, не выражаемые матрицей.

ЧТО СДЕЛАТЬ
1. src/lib/permissions.ts: матрица прав по ролям, функции can(user, permission)
   и requirePermission(user, permission). Поддержи шаблон вида "users.*".
2. Добавь requirePermission в каждый server action и в каждую выборку данных
   раздела пользователей и журнала. Проверка права идёт до валидации данных.
3. Закрой страницы: сотрудник и подрядчик, зайдя на /users или /audit, попадают
   на страницу «Доступ запрещён», а не видят пустой список.
4. В интерфейсе скрывай недоступное той же функцией can() — отдельного списка
   правил для UI быть не должно.
5. Проверь правила из раздела 3 документа о правах: последний администратор,
   действия над собой, режим только для чтения у менеджера.
6. Напиши модульные тесты на permissions.ts: по одному случаю разрешения
   и запрета для каждой роли.

КРИТЕРИИ ПРИЁМКИ
- Менеджер открывает /users, но не видит кнопок создания, редактирования,
  сброса пароля и деактивации.
- Вызов действия создания пользователя от имени менеджера в обход интерфейса
  отклоняется сервером.
- Сотрудник и подрядчик получают «Доступ запрещён» на /users и /audit,
  но открывают /profile.
- Понижение роли последнего администратора отклоняется.
- Тесты прав проходят.

ЧЕГО НЕ ДЕЛАТЬ
- Не добавляй прав для разделов, которых ещё нет.
- Не заводи отдельную таблицу прав в базе: на этом этапе матрица задаётся в коде.
```

---

## Шаг 6. Аудит

```
ЦЕЛЬ
Журналировать все действия и показывать историю изменений.

ИСТОЧНИК
docs/ТЗ.md, разделы 4.9 и 4.10. docs/СХЕМА-БД.md, раздел 4 — правила записи.

ЧТО СДЕЛАТЬ
1. src/lib/audit/index.ts:
   - logAudit({ actor, action, entity, entityId, summary, changes, ip, userAgent }),
     записывает в AuditLog в той же транзакции, что и само изменение;
   - diffEntity(before, after), возвращает массив { field, before, after }
     только по изменившимся полям;
   - явная константа исключаемых полей: passwordHash, password, newPassword,
     tokenHash. Не определяй чувствительные поля по подстроке в названии.
   - actorLogin всегда записывается строкой, рядом с actorId.
2. Подключи журналирование ко всем действиям: вход, неудачный вход, выход,
   создание и изменение пользователя, смена роли, сброс пароля,
   деактивация и активация, завершение сессии администратором.
   Сброс пароля пишется как факт, без значений в changes.
3. Ссылочные значения в changes записывай читаемым текстом: не код роли,
   а её название на русском.
4. Страница /audit: таблица с фильтрами по автору, действию, типу сущности
   и периоду; сортировка по времени; разворачивающаяся строка с деталями
   «поле: было → стало»; серверная пагинация.
5. Вкладка «История изменений» в карточке пользователя — записи журнала
   по этой сущности.
6. Отметки в карточках: «Создано: дата, автор · Изменено: дата, автор».
7. Модульные тесты: diffEntity не пропускает чувствительные поля и не включает
   неизменившиеся.

КРИТЕРИИ ПРИЁМКИ
- Любое изменение пользователя порождает запись со списком изменённых полей
  и значениями «было → стало».
- Пароли, их хэши и токены не встречаются в журнале ни в каком виде.
- Неудачная попытка входа записывается с введённым логином и IP,
  но без пароля.
- Журнал фильтруется по автору, действию и периоду.
- После деактивации пользователя его записи в журнале остаются читаемыми
  и показывают логин.
- В карточке видны корректные отметки «Создано» и «Изменено».
- Изменение записи журнала невозможно ни через один экран приложения.

ЧЕГО НЕ ДЕЛАТЬ
- Не добавляй удаление и правку записей журнала.
- Не логируй просмотры страниц: журналируются изменения данных, входы и выгрузки.
```

---

## Шаг 7. Профиль и сессии

```
ЦЕЛЬ
Дать каждому пользователю свой профиль и управление собственными сессиями.

ИСТОЧНИК
docs/ТЗ.md, раздел 4.8.

ЧТО СДЕЛАТЬ
1. Страница /profile, доступная всем ролям: просмотр своих данных.
   Редактирование ФИО, должности, email и телефона — только администратором
   или менеджером; сотрудник и подрядчик свой профиль только просматривают.
   Логин, роль и пароль недоступны для изменения.
2. Администратор и менеджер правят и профили других пользователей — в форме
   /users/[id]/edit. Менеджеру доступны только ФИО, должность, email и телефон;
   профиль администратора правит только администратор.
3. Раздел «Активные сессии» в профиле: устройство и браузер, IP, начало,
   последняя активность, пометка «текущая». Кнопки «Завершить сессию»
   и «Завершить все, кроме текущей».
4. Ссылка на профиль в шапке приложения.

КРИТЕРИИ ПРИЁМКИ
- Пользователь любой роли открывает /profile; администратор и менеджер правят
  свои данные, сотрудник и подрядчик видят их без возможности правки.
- Менеджер правит ФИО, должность, email и телефон сотрудника, но не может
  править профиль администратора — ни в интерфейсе, ни в обход него.
- Изменение своего профиля попадает в журнал аудита.
- Завершение конкретной сессии немедленно выбрасывает соответствующий браузер
  на /login.

ЧЕГО НЕ ДЕЛАТЬ
- Не давай пользователю менять свою роль, логин или пароль.
- Не добавляй загрузку аватара и уведомления — их нет в ТЗ.
```

---

## Шаг 8. Экспорт и печать

```
ЦЕЛЬ
Сделать общий механизм выгрузки, которым воспользуются все будущие разделы.

ИСТОЧНИК
docs/ТЗ.md, раздел 4.11. docs/АРХИТЕКТУРА.md, раздел 5.

ЧТО СДЕЛАТЬ
1. src/lib/export с единым описанием колонок:
   тип ExportColumn { key, header, width?, format? }, где format —
   text | number | money | date | datetime.
2. Выгрузка в Excel через ExcelJS: заголовки на русском, закреплённая первая
   строка, автоширина колонок, даты — датами, числа — числами, а не строками.
3. Выгрузка в PDF через @react-pdf/renderer: альбомная ориентация, шапка
   с названием отчёта, датой выгрузки и автором, нумерация страниц, повтор
   заголовков таблицы на каждой странице. Подключи кириллический шрифт и убедись,
   что русский текст отображается, а не заменяется прямоугольниками.
4. Представление для печати: отдельный маршрут с печатной таблицей,
   корректными разрывами страниц и вызовом печати браузера.
5. Подключи выгрузку к реестру пользователей и к журналу аудита. Выгружается то,
   что отобрано текущими фильтрами, а не вся таблица: передавай в выгрузку
   те же параметры, что и в реестр.
6. Каждая выгрузка пишется в аудит как EXPORT с указанием раздела, формата
   и числа строк.
7. Имя файла — вида users_14.09.2026.xlsx.

КРИТЕРИИ ПРИЁМКИ
- Выгрузка реестра с применённым фильтром по роли содержит только отфильтрованные
  строки.
- Excel-файл открывается без предупреждений, даты распознаются как даты,
  заголовки на русском.
- В PDF кириллица отображается корректно, заголовки повторяются на второй
  странице, есть нумерация.
- Печатное представление печатается без обрезанных колонок.
- Каждая выгрузка появляется в журнале аудита.
- Добавление выгрузки к новому разделу требует только описания колонок.

ЧЕГО НЕ ДЕЛАТЬ
- Не делай отдельную реализацию выгрузки для каждого раздела.
- Не выгружай все записи в обход фильтров.
- Не включай в выгрузку служебные поля: хэши, идентификаторы сессий.
```

---

## Шаг 9. Тесты

```
ЦЕЛЬ
Закрыть тестами то, что дороже всего чинить после поломки: права, аудит, вход.

ЧТО СДЕЛАТЬ
1. Модульные тесты (Vitest):
   - permissions.ts: разрешение и запрет для каждой из четырёх ролей;
   - diffEntity: не включает неизменившиеся поля, не включает чувствительные;
   - валидация пароля и нормализация логина в схемах Zod;
   - правило последнего администратора.
2. Сквозные тесты (Playwright) в двух проектах — Chromium и WebKit (движок Safari,
   ТЗ п. 3.2); сценарий входа дополнительно на мобильном профиле шириной 360 px:
   - вход с верными и неверными данными;
   - создание пользователя администратором и вход под ним;
   - менеджер не видит кнопок создания, сброса пароля и деактивации
     и не может править профиль администратора;
   - деактивация пользователя прекращает его сессию.
3. Подготовь отдельную тестовую базу и команду её инициализации, чтобы тесты
   не работали с базой разработчика.
4. Опиши запуск тестов в README.

КРИТЕРИИ ПРИЁМКИ
- npm test и npm run test:e2e проходят на чистой базе.
- Тесты не зависят от порядка выполнения и от данных, оставшихся от прошлого
  запуска.
- Тесты не обращаются к базе разработчика.

ЧЕГО НЕ ДЕЛАТЬ
- Не гонись за процентом покрытия: тесты на верстку и тривиальные геттеры
  не нужны.
- Не подменяй базу заглушками в тестах прав и аудита — они должны проверять
  реальное поведение.
```

---

## Шаг 10. Приёмка этапа 1

```
ЦЕЛЬ
Убедиться, что этап 1 завершён, и подготовить документацию.

ЧТО СДЕЛАТЬ
1. Пройди чек-лист приёмки из docs/ТЗ.md, раздел 4.12 — все 19 пунктов,
   вручную в браузере. Составь отчёт: пункт — результат — замечание.
2. Исправь найденные несоответствия.
3. Проверь, что в коде не осталось русских строк вне messages/ru.json.
4. Дополни README: назначение, стек, запуск, структура проекта, команды,
   первый вход в систему.
5. Убедись, что npm run lint, npm run typecheck, npm test и npm run test:e2e
   проходят.
6. Составь список технического долга и решений, отложенных на потом,
   в конце docs/РОАДМАП.md.

КРИТЕРИИ ПРИЁМКИ
- Все пункты чек-листа пройдены или по каждому непройденному есть объяснение
  и план.
- Новый разработчик поднимает проект по README без дополнительных вопросов.

ЧЕГО НЕ ДЕЛАТЬ
- Не начинай разделы этапа 2, пока этап 1 не принят.
```

---

# Этап 2. Раздел «Настройки»: блок «Наша компания»

Шаги 11–16. Требования — `docs/ТЗ.md`, раздел 5; модели — `docs/СХЕМА-БД.md`,
раздел 8; права — `docs/ПРАВА-ДОСТУПА.md`; устройство раздела из блоков —
`docs/АРХИТЕКТУРА.md`, п. 3.7. Образцы экранов — скриншоты «Сведения о компании»
и «Редактировать данные компании»; приложите их к сообщению на шагах 13 и 14.

---

## Шаг 11. Схема данных и валидация

```
Продолжаем разработку ConstructionProjects. Этап 1 принят, начинаем этап 2 —
раздел «Настройки» с блоком «Наша компания». Прочитай CLAUDE.md, docs/ТЗ.md
(раздел 5), docs/СХЕМА-БД.md (раздел 8) и docs/АРХИТЕКТУРА.md (п. 3.7).

ЦЕЛЬ
Модель данных сведений о компании и единая схема валидации формы.
Интерфейса на этом шаге нет.

ЧТО СДЕЛАТЬ
1. Опиши в prisma/schema.prisma перечисления LegalForm, CompanyAddressType,
   SocialNetwork и модели CompanyProfile, CompanyAddress, CompanyPhone,
   CompanySocialLink, CompanyActivity ровно по docs/СХЕМА-БД.md, 8.2,
   с обратными связями у User.
2. Создай миграцию с --create-only (имя company_profile), допиши в её SQL
   CHECK ("singleton" = true) и частичный уникальный индекс для OFFICE и POSTAL
   (СХЕМА-БД, 8.3), затем примени её. Покажи дописанный SQL в отчёте.
3. src/features/company/nl-identifiers.ts — чистые функции нормализации и проверки:
   KvK-nummer, vestigingsnummer, RSIN с elfproef, btw-id и omzetbelastingnummer,
   loonheffingennummer, нидерландский postcode (→ "1234 AB", без SA/SD/SS),
   телефон (→ "+31…", правила ТЗ п. 5.4), адрес сайта (только http/https или без схемы).
4. src/features/company/countries.ts — список кодов стран формы (ТЗ п. 5.4)
   и функция названия страны через Intl.DisplayNames по текущей локали.
5. src/features/company/schemas.ts — одна схема Zod формы со всеми группами
   ТЗ п. 5.4: обязательность, длины, пределы списков (склады 10, телефоны 5,
   соцсети 10, виды деятельности 20), неповторяющиеся коды SBI, ровно один основной
   вид деятельности при непустом списке, почтовый адрес — либо флажок «совпадает»,
   либо адрес, либо абонентский ящик, поле version. Индекс проверяется по правилам
   Нидерландов только при стране NL. Сообщения ошибок — ключи messages/ru.json.
6. Модульные тесты (tests/unit): каждый формат — верное значение, неверное значение,
   нормализация; elfproef на заведомо верном и неверном RSIN; телефоны "06 84 61 47 32",
   "0031 6 84614732", "+31684614732"; индекс "3741rs" → "3741 RS" и отказ для "0123 AB"
   и "1234 SS"; сайт "javascript:alert(1)" отклоняется; правило одного основного
   вида деятельности.

КРИТЕРИИ ПРИЁМКИ
- npx prisma migrate dev проходит на чистой базе; попытка вставить вторую запись
  CompanyProfile или второй активный адрес офиса отклоняется базой.
- Все тесты шага проходят; npm run lint и npm run typecheck — без ошибок.
- В схемах Zod нет русских строк — только ключи переводов.

ЧЕГО НЕ ДЕЛАТЬ
- Не создавай страниц, компонентов и server actions — это шаги 12–14.
- Не добавляй библиотек для телефонов, стран или валидации номеров: всё решается
  регулярными выражениями, Intl и Zod.
- Не редактируй уже применённые миграции этапа 1.
```

---

## Шаг 12. Права, навигация и каркас раздела

```
ЦЕЛЬ
Раздел «Настройки» появляется в приложении для администратора и менеджера
и закрыт для остальных.

ИСТОЧНИК
docs/ТЗ.md, пп. 5.2, 5.3 и 5.6 (пустое состояние); docs/ПРАВА-ДОСТУПА.md —
строки settings.* в матрице и правила 8–9; docs/АРХИТЕКТУРА.md, п. 3.7.

ЧТО СДЕЛАТЬ
1. src/lib/permissions.ts: права settings.read, settings.company.read,
   settings.company.update, settings.company.history; матрица — как в
   docs/ПРАВА-ДОСТУПА.md, раздел 4. Дополни модульные тесты прав.
2. src/lib/audit: "CompanyProfile" в AUDIT_ENTITIES, его название «Сведения
   о компании» для фильтра журнала; в HISTORY_PERMISSION —
   settings.company.history.
3. Боковое меню: последний пункт «Настройки» (пиктограмма Settings) по праву
   settings.read. Главная: карточка «Настройки» по тому же праву.
4. src/features/company/queries.ts: getCompanyProfile(actor) с проверкой
   settings.company.read; адреса без deletedAt, списки по sortOrder.
5. Страница /settings: requirePagePermission("settings.read"), заголовок
   «Настройки», список блоков раздела. Блок «Наша компания» — серверный компонент,
   выводится при праве settings.company.read. На этом шаге блок показывает
   шапку (пиктограмма, заголовок «Сведения о компании») и пустое состояние
   «Сведения о компании ещё не заполнены»; кнопка «Заполнить» пока неактивна.
6. Все тексты — в messages/ru.json, раздел settings (settings.company.*).

КРИТЕРИИ ПРИЁМКИ
- Администратор и менеджер видят пункт меню и карточку, /settings открывается
  с пустым состоянием блока.
- Сотрудник и подрядчик не видят ни пункта, ни карточки; /settings показывает
  «Доступ запрещён».
- В фильтре типа сущности журнала /audit есть «Сведения о компании».
- Проверено в браузере на 1280, 768 и 360 px: пункт меню в свёрнутом сайдбаре
  показывается пиктограммой с подсказкой.
- npm run lint, npm run typecheck, npm test проходят.

ЧЕГО НЕ ДЕЛАТЬ
- Не делай форму и вывод заполненных сведений — шаги 13 и 14.
- Не добавляй заглушки будущих блоков раздела.
```

---

## Шаг 13. Диалог заполнения и сохранение

```
ЦЕЛЬ
Администратор и менеджер заполняют и правят сведения о компании в диалоге;
каждое сохранение проверяется, пишется в журнал и защищено от одновременной правки.

ИСТОЧНИК
docs/ТЗ.md, пп. 5.4, 5.5, 5.7, 5.8; docs/СХЕМА-БД.md, 8.3–8.4.
Образец диалога — приложенный скриншот «Редактировать данные компании».

ЧТО СДЕЛАТЬ
1. src/features/company/actions.ts — saveCompanyProfile через
   authorizedAction("settings.company.update"): право → схема Zod из шага 11 →
   транзакция:
   - первое сохранение создаёт CompanyProfile (CREATE), дальнейшие обновляют
     с условием по version и увеличивают её (UPDATE); ноль обновлённых строк —
     результат { ok: false, error: "settings.company.errors.concurrentUpdate" };
   - адреса офиса, почтовый и склады обновляются на месте по id, новые создаются,
     удалённые склады и ненужный почтовый адрес получают deletedAt;
   - телефоны, соцсети и виды деятельности заменяются целиком;
   - createdById/updatedById у профиля и адресов;
   - сохранение без изменений ничего не пишет и не увеличивает version;
   - revalidatePath("/settings").
   Одновременное первое сохранение двумя пользователями не должно падать ошибкой
   базы: нарушение уникальности singleton превращается в ту же ошибку
   одновременной правки.
2. src/features/company/audit.ts — снимок «до/после» с читаемыми значениями
   (правовая форма названием, дата dd.MM.yyyy, страна названием, «Да»/«Нет»,
   адрес одной строкой, списки через «; ») и diffEntity из src/lib/audit.
   summary — «Заполнены сведения о компании» / «Изменены сведения о компании».
3. Диалог CompanyProfileDialog (клиентский, react-hook-form + zodResolver с той же
   схемой, useFieldArray для списков):
   - порядок и раскладка полей первой части — как на образце, далее группы
     «Реквизиты», «Почтовый адрес», «Склады», «Дополнительные телефоны»,
     «Социальные сети», «Виды деятельности» с подзаголовками и разделителями;
   - звёздочки у обязательных полей, подсказка о разнице btw-id и
     omzetbelastingnummer, в подписях нидерландские термины в скобках;
   - закреплённые шапка и панель «Отмена»/«Сохранить», прокрутка содержимого;
     на ширине меньше 768 px — на весь экран, поля в одну колонку;
   - фокус в «Название компании» при открытии; подтверждение закрытия при
     несохранённых изменениях; блокировка «Сохранить» на время отправки;
     прокрутка к первой ошибке; ошибки сервера — под полями;
   - ошибка одновременной правки — сообщением в диалоге, ввод не сбрасывается;
   - успех — закрытие диалога и уведомление «Сведения о компании сохранены».
   Используй существующие компоненты src/components/ui (dialog, field, input,
   select, checkbox, textarea), не переписывая их.
4. Подключи диалог к кнопке «Заполнить» пустого состояния и к кнопке
   «Редактировать» в шапке блока (кнопка — только при settings.company.update).
5. Тесты на тестовой базе (tests/db): сохранение от имени сотрудника отклоняется
   до валидации; неверные данные при прямом вызове отклоняются с ошибками полей;
   CREATE при первом сохранении и UPDATE с изменёнными полями при следующем;
   сохранение без изменений не пишет журнал; устаревшая version даёт ошибку
   одновременной правки; удалённый склад получает deletedAt.

КРИТЕРИИ ПРИЁМКИ
- Критерии 4, 5, 6, 10, 11 и 12 из docs/ТЗ.md, п. 5.10 проверены в браузере.
- Одновременная правка проверена в двух браузерах: изменения первого не затёрты.
- В журнале /audit нет version, sortOrder и идентификаторов в изменениях.
- npm run lint, npm run typecheck, npm test проходят.

ЧЕГО НЕ ДЕЛАТЬ
- Не выноси правку на отдельный маршрут: по образцу это диалог.
- Не делай отдельных действий и отдельных прав для групп полей.
- Не добавляй автозаполнение по KvK, справочник SBI, загрузку логотипа.
```

---

## Шаг 14. Вывод сведений и история изменений

```
ЦЕЛЬ
Главная страница раздела показывает сведения о компании в раскладке образца;
есть история изменений блока.

ИСТОЧНИК
docs/ТЗ.md, пп. 5.6 и 5.8. Образец — приложенный скриншот «Сведения о компании».

ЧТО СДЕЛАТЬ
1. Шапка блока: пиктограмма шестерёнки, «Сведения о компании», ссылка
   «История изменений» (при settings.company.history) и кнопка «Редактировать»
   с карандашом; под заголовком — RecordStamps «Создано … · Изменено …».
2. Группа «Основное» — как на образце: рамка, таблица «название — значение»
   в две колонки (слева название, адрес офиса, email, телефон; справа сайт,
   KvK-nummer, btw-id, omzetbelastingnummer), название полужирным слева,
   значение справа, строки разделены линиями.
3. Группы «Реквизиты», «Адреса», «Контакты», «Виды деятельности» в том же
   оформлении; пустая группа не выводится; незаполненное значение — «—».
   Оформление строки «название — значение» сделай одним компонентом для всех групп.
4. Форматирование: адрес одной строкой в нидерландском порядке (страна — только
   не для NL), Postbus, дата регистрации как дата UTC, основной вид деятельности
   с меткой «Основной». Функция форматирования адреса — одна, её же использует
   audit.ts из шага 13.
5. Ссылки: mailto:, tel:, сайт и соцсети — в новой вкладке с
   rel="noopener noreferrer"; к сайту без схемы добавляется https://.
6. Адаптивность по ТЗ п. 5.6: две колонки от 1280 px, одна — уже; на 360 px
   название над значением, длинные значения переносятся.
7. Страница /settings/company/history: requirePagePermission
   ("settings.company.history"), ссылка «Назад» на /settings, AuditTable
   с listEntityAuditLogs("CompanyProfile", id); IP и браузер — только при audit.read.
   Пока сведений нет — пустое состояние истории.

КРИТЕРИИ ПРИЁМКИ
- Критерии 3, 7, 8, 9, 13 и 14 из docs/ТЗ.md, п. 5.10 проверены в браузере.
- Скриншоты блока на 1280, 768 и 360 px приложены к отчёту; горизонтальной
  прокрутки страницы нет.
- Менеджер открывает историю и не видит IP-адресов; сотрудник получает
  «Доступ запрещён».
- npm run lint, npm run typecheck, npm test проходят.

ЧЕГО НЕ ДЕЛАТЬ
- Не копируй таблицу истории и выборку журнала в features/company.
- Не добавляй выгрузку и печать сведений о компании: их нет в ТЗ этапа 2.
```

---

## Шаг 15. Сквозные тесты этапа 2

```
ЦЕЛЬ
Закрыть сквозными тестами доступ к разделу и основной сценарий блока.

ЧТО СДЕЛАТЬ
1. Playwright (Chromium и WebKit, как на шаге 9):
   - сотрудник не видит пункта «Настройки», /settings и
     /settings/company/history дают «Доступ запрещён»;
   - менеджер заполняет сведения с нуля: обязательные поля, склад, дополнительный
     телефон, вид деятельности; видит их в блоке с нормализованными индексом,
     телефоном и btw-id; запись появляется в истории изменений;
   - ошибка формата (KvK из 7 цифр) показывается под полем и не закрывает диалог;
   - закрытие диалога с изменениями запрашивает подтверждение;
   - два контекста браузера открывают диалог, второй сохраняет после первого
     и получает сообщение об одновременной правке.
2. На мобильном профиле 360 px: диалог открывается на весь экран, кнопка
   «Сохранить» видна без прокрутки, у страницы /settings нет горизонтальной
   прокрутки (scrollWidth документа не больше ширины окна).
3. Тесты не зависят друг от друга: запись CompanyProfile одна на базу, поэтому
   тесты, которые её меняют, очищают её перед собой и не идут параллельно
   (test.describe.configure({ mode: "serial" }) или отдельный файл с одним воркером).

КРИТЕРИИ ПРИЁМКИ
- npm test и npm run test:e2e проходят на чистой базе дважды подряд.
- Тесты не обращаются к базе разработчика.

ЧЕГО НЕ ДЕЛАТЬ
- Не проверяй в e2e то, что уже покрыто модульными тестами форматов шага 11.
```

---

## Шаг 16. Приёмка этапа 2

```
ЦЕЛЬ
Убедиться, что этап 2 завершён, и привести документацию в порядок.

ЧТО СДЕЛАТЬ
1. Пройди чек-лист docs/ТЗ.md, п. 5.10 — все 16 пунктов, вручную в браузере,
   под администратором, менеджером и сотрудником. Отчёт: пункт — результат —
   замечание.
2. Сверь экраны со скриншотами-образцами и перечисли отличия.
3. Исправь найденные несоответствия.
4. Проверь, что в коде нет русских строк вне messages/ru.json.
5. Обнови README: раздел «Настройки» в описании готового, роли.
6. Убедись, что npm run lint, npm run typecheck, npm test, npm run test:e2e
   и npm run format:check проходят.
7. Дополни технический долг в docs/РОАДМАП.md решениями, отложенными на этапе 2.

КРИТЕРИИ ПРИЁМКИ
- Все пункты чек-листа пройдены или по каждому непройденному есть объяснение и план.
- Документы docs/ соответствуют реализованному.

ЧЕГО НЕ ДЕЛАТЬ
- Не начинай этап 3, пока этап 2 не принят.
```

---

# Этап 3. Справочники и раздел «Проекты»

Шаги 17–25. Требования — `docs/ТЗ.md`, раздел 6; модели — `docs/СХЕМА-БД.md`, раздел 9;
права — `docs/ПРАВА-ДОСТУПА.md`, строки `customers.*`, `contractors.*`, `projects.*`,
`users.changeContractor` и правила 10–15; соглашения — `docs/АРХИТЕКТУРА.md`, пп. 3.8–3.9.

Порядок выбран так, чтобы каждый шаг проверялся в браузере: сначала общие модули и схема,
затем права и навигация, потом справочники (они нужны форме проекта) и сам проект.

---

## Шаг 17. Общие нидерландские проверки и схема данных

```
Продолжаем разработку ConstructionProjects. Этап 2 принят, начинаем этап 3 —
справочники заказчиков и подрядчиков и раздел «Проекты». Прочитай CLAUDE.md,
docs/ТЗ.md (раздел 6), docs/СХЕМА-БД.md (раздел 9) и docs/АРХИТЕКТУРА.md (пп. 3.8–3.9).

ЦЕЛЬ
Общие модули проверок вынесены из блока «Наша компания», модели этапа 3 созданы,
схемы Zod и расчёты проекта покрыты тестами. Интерфейса на этом шаге нет.

ЧТО СДЕЛАТЬ
1. Перенеси из src/features/company в src/lib/nl без изменения поведения:
   nl-identifiers.ts, countries.ts, форматирование адреса одной строкой (address.ts)
   и фрагмент схемы Zod адреса с правилом «целиком или никак» (из schemas.ts).
   Ключи общих ошибок перенеси из settings.company.validation в раздел validation
   messages/ru.json. Блок «Наша компания» использует новые модули; модульные тесты
   шага 11 переезжают вместе с кодом и проходят без правок ожиданий.
2. Опиши в prisma/schema.prisma перечисления CustomerType, ProjectStatus, VatRate,
   модели Customer, Contractor, Project, поле User.contractorId и обратные связи
   ровно по docs/СХЕМА-БД.md, 9.1–9.2.
3. Создай миграцию с --create-only (имя customers_contractors_projects), допиши в SQL
   CHECK-ограничения и частичный уникальный индекс номера проекта из СХЕМА-БД, 9.3,
   затем примени. Покажи дописанный SQL в отчёте.
4. src/features/customers/schemas.ts и src/features/contractors/schemas.ts — схемы форм
   по ТЗ пп. 6.4–6.5. У частного лица KvK-nummer, btw-id и контактное лицо обнуляются.
5. src/features/projects/schemas.ts — схема формы проекта по ТЗ п. 6.6: номер
   (1–20, латиница, цифры, «-», «.», «/»), обязательный адрес объекта, дата начала,
   бюджет — строка с запятой или точкой и разделителями разрядов → десятичное значение
   с точностью до цента, ставка НДС обязательна при указанном бюджете, часы.
   Отдельная схема привязки подрядчика в форме пользователя: contractorId допустим
   только при роли CONTRACTOR.
6. src/features/projects/budget.ts — сумма с НДС в целых центах, округление «половина
   вверх». src/features/projects/duration.ts — длительность в днях по ТЗ п. 6.6
   (сегодня по Europe/Kyiv, у закрытого — дата closedAt по Europe/Kyiv, «не начат»).
   src/features/projects/number.ts — чистая функция следующего номера «ГГГГ-NNN»
   по списку существующих номеров.
7. Модульные тесты: бюджет 12500,5 при 21 % → 15 125,61, при 9 %, 0 % и Verlegd;
   ввод «12.500,50», «12 500,5», отрицательная сумма и три знака после запятой отклоняются;
   длительность: начат сегодня — 1, вчера — 2, завтра — «не начат», закрыт 10 дней назад
   не растёт; номер: пустой год → 2026-001, после 2026-009 → 2026-010, после 2026-999 →
   2026-1000, чужие форматы и прошлый год не учитываются; частное лицо теряет KvK;
   бюджет без ставки отклоняется.

КРИТЕРИИ ПРИЁМКИ
- npx prisma migrate dev проходит на чистой базе; база отклоняет проект с бюджетом без
  ставки, два неудалённых проекта с номерами «A-1» и «a-1»,
  закрытый проект без closedAt, частное лицо с KvK-nummer.
- Блок «Наша компания» работает как раньше: npm test, включая тесты шагов 11–13, проходит.
- npm run lint и npm run typecheck — без ошибок; в схемах Zod нет русских строк.

ЧЕГО НЕ ДЕЛАТЬ
- Не создавай страниц, компонентов и server actions — это шаги 18–23.
- Не меняй правила проверок этапа 2 при переносе.
- Не вводи Float и арифметику с плавающей точкой для денег и часов.
- Не редактируй применённые миграции этапов 1–2.
```

---

## Шаг 18. Права, навигация и каркас разделов

```
ЦЕЛЬ
Разделы «Проекты», «Заказчики», «Подрядчики» появляются в меню по ролям и закрыты
для тех, кому недоступны.

ИСТОЧНИК
docs/ТЗ.md, пп. 6.2–6.3; docs/ПРАВА-ДОСТУПА.md — строки этапа 3 и правила 10–15.

ЧТО СДЕЛАТЬ
1. src/lib/permissions.ts: коды прав customers.*, contractors.*, projects.*,
   users.changeContractor и матрица по docs/ПРАВА-ДОСТУПА.md, раздел 4. Модульные тесты:
   разрешение и запрет каждой новой строки матрицы для четырёх ролей.
2. src/lib/audit: "Customer", "Contractor", "Project" в AUDIT_ENTITIES с названиями
   для фильтра журнала; HISTORY_PERMISSION — customers.history, contractors.history,
   projects.history.
3. Боковое меню: «Главная», «Проекты», «Заказчики», «Подрядчики», «Пользователи»,
   «Журнал аудита», «Настройки» — пункты по правам (Проекты — projects.readActive).
   Главная: карточка «Проекты» активна, карточки «Заказчики» и «Подрядчики» — по правам;
   «Сметы» и «Дашборд» остаются неактивными.
4. Страницы-каркасы /projects, /customers, /contractors с requirePagePermission и
   заголовком раздела; содержимое — на шагах 19–21.
5. Все тексты — в messages/ru.json, разделы projects, customers, contractors.

КРИТЕРИИ ПРИЁМКИ
- Администратор и менеджер видят три новых пункта; сотрудник — тоже; подрядчик — только
  «Проекты»; /customers и /contractors у подрядчика — «Доступ запрещён».
- В фильтре типа сущности /audit есть «Заказчик», «Подрядчик», «Проект».
- В свёрнутом сайдбаре на 768 и 360 px новые пункты показываются пиктограммами с подсказкой.
- npm run lint, npm run typecheck, npm test проходят.

ЧЕГО НЕ ДЕЛАТЬ
- Не делай реестров и форм — шаги 19–23.
- Не добавляй прав на расходы, часы и сметы: это этап 4 и дальше.
```

---

## Шаг 19. Справочник заказчиков

```
ЦЕЛЬ
Полная работа с заказчиками: реестр, создание, правка, архив, карточка, история, выгрузка.

ИСТОЧНИК
docs/ТЗ.md, пп. 6.4 и 6.10; docs/АРХИТЕКТУРА.md, п. 3.8. Образец устройства —
раздел пользователей (src/features/users, src/app/(app)/users).

ЧТО СДЕЛАТЬ
1. src/features/customers/queries.ts — список с поиском, фильтрами вида и статуса
   (по умолчанию активные), сортировкой и серверной пагинацией; одна запись; список
   активных заказчиков для выбора в форме проекта (с добавлением уже выбранного архивного).
   Каждая выборка принимает actor и проверяет право.
2. src/features/customers/actions.ts — createCustomer, updateCustomer, changeCustomerStatus
   через authorizedAction: право → схема Zod шага 17 → транзакция с записью и logAudit.
   Занятый KvK-nummer — ошибка поля, а не сбой. Сохранение без изменений ничего не пишет.
   Повторный перевод в архив уже архивного заказчика ничего не пишет.
3. src/features/customers/audit.ts — читаемые значения: вид названием, адрес одной строкой
   функцией из src/lib/nl, «Активен» / «В архиве».
4. Реестр /customers по ТЗ п. 6.4: колонки, поиск с задержкой 300 мс, фильтры, сортировка,
   пагинация, состояние в URL, пустые состояния, кнопки и строчные действия по правам.
5. Формы /customers/new и /customers/[id]/edit на react-hook-form с той же схемой;
   переключение вида скрывает поля компании.
6. Карточка /customers/[id]: вкладки «Сведения» (RecordStamps, значения в оформлении
   строк «название — значение» блока «Наша компания» — переиспользуй компонент, а не копию),
   «Проекты» (пока пустое состояние; список появится на шаге 21), «История изменений»
   (AuditTable и listEntityAuditLogs).
7. Архив и восстановление — с диалогом подтверждения, в котором назван заказчик.
8. Выгрузка: features/customers/export.ts через defineExportReport, отчёт в
   features/export/reports.ts, кнопки ExportButtons на реестре.
9. Тесты tests/db: создание от имени сотрудника отклоняется до валидации; неверный KvK
   при прямом вызове — ошибка поля; занятый KvK; CREATE, UPDATE с изменёнными полями,
   STATUS_CHANGE; сохранение без изменений не пишет журнал.

КРИТЕРИИ ПРИЁМКИ
- Критерии 2, 3, 4 и часть 5 (архив и восстановление заказчика) из docs/ТЗ.md, п. 6.12
  проверены в браузере.
- Сотрудник открывает реестр, карточку и историю, но не видит кнопок правки, архива
  и выгрузки.
- Выгрузка в Excel учитывает фильтры и появляется в журнале.
- Проверено на 1280, 768 и 360 px без горизонтальной прокрутки страницы.
- npm run lint, npm run typecheck, npm test проходят.

ЧЕГО НЕ ДЕЛАТЬ
- Не делай удаления заказчиков: только архив.
- Не копируй проверки номеров и адреса — они в src/lib/nl.
- Не добавляй автозаполнение по KvK и импорт заказчиков.
```

---

## Шаг 20. Справочник подрядчиков и привязка учётных записей

```
ЦЕЛЬ
Справочник подрядчиков и привязка учётных записей с ролью «Подрядчик» к организации.

ИСТОЧНИК
docs/ТЗ.md, пп. 6.5 и 6.10; docs/ПРАВА-ДОСТУПА.md, правила 13–15.

ЧТО СДЕЛАТЬ
1. src/features/contractors — queries, actions, audit, export, компоненты реестра,
   форм и карточки по образцу шага 19, без фильтра вида. Общие части реестра и карточки
   справочника, повторившиеся в заказчиках и подрядчиках, вынеси в общий компонент,
   а не копируй.
2. Карточка /contractors/[id]: «Сведения», «Учётные записи» (логин, ФИО, статус, ссылки;
   вкладка только при users.read), «История изменений».
3. Форма пользователя /users/new и /users/[id]/edit: поле «Организация» (выбор из
   активных подрядчиков с поиском) — только при роли CONTRACTOR и праве
   users.changeContractor. createUser и updateUser: передача contractorId без права
   отклоняется errors.forbiddenAction; архивный подрядчик отклоняется, если значение
   меняется; при смене роли на другую contractorId обнуляется в той же транзакции.
   Изменение привязки пишется в UPDATE пользователя полем contractor с названием.
4. Карточка пользователя: организация ссылкой на карточку подрядчика.
5. Тесты tests/db: менеджер не может передать contractorId; архивного подрядчика нельзя
   выбрать; смена роли снимает привязку и пишет это в журнал; STATUS_CHANGE подрядчика.

КРИТЕРИИ ПРИЁМКИ
- Критерий 6 и часть критерия 5 (архив подрядчика) из docs/ТЗ.md, п. 6.12 проверены
  в браузере.
- Уже привязанная к архивному подрядчику учётная запись сохраняет привязку и правится
  без ошибки.
- npm run lint, npm run typecheck, npm test проходят.

ЧЕГО НЕ ДЕЛАТЬ
- Не ограничивай видимость проектов по организации подрядчика: по ТЗ п. 6.5 привязка
  на доступ этапа 3 не влияет.
- Не добавляй назначение подрядчиков на проекты.
```

---

## Шаг 21. Реестр проектов

```
ЦЕЛЬ
Реестр проектов с разным набором колонок и строк для разных ролей и выгрузкой.

ИСТОЧНИК
docs/ТЗ.md, пп. 6.2, 6.6, 6.8; docs/ПРАВА-ДОСТУПА.md, правила 11–12;
docs/АРХИТЕКТУРА.md, п. 3.9.

ЧТО СДЕЛАТЬ
1. src/features/projects/queries.ts — listProjects(actor, params):
   - без projects.read — только status = IN_PROGRESS и поля подрядчика, фильтры статуса
     и заказчика игнорируются;
   - без projects.budget.read — поля бюджета не выбираются из базы;
   - удалённые проекты исключаются всегда;
   - поиск по номеру, названию, заказчику и городу; фильтры статуса (по умолчанию
     «В работе») и заказчика; сортировка по ТЗ п. 6.8, по умолчанию — дата начала, новые
     сверху; серверная пагинация.
   Тип результата отражает набор полей: бюджет необязателен, у подрядчика нет заказчика.
2. Колонки реестра и выгрузки — одна функция от прав пользователя (ТЗ, таблица п. 6.8).
   Длительность — функцией шага 17, суммы — форматом src/lib/format.ts со знаком €.
3. Страница /projects: DataTable, панель поиска и фильтров (у подрядчика без фильтров),
   кнопка «Создать проект» по праву (ведёт на /projects/new, форма — шаг 22),
   строчные действия «Открыть», «Редактировать» (только проект «В работе»).
4. Выгрузка: features/projects/export.ts через defineExportReport; бюджет без НДС, ставка,
   бюджет с НДС, часы — только при projects.budget.read; суммы — format "money".
5. Вкладка «Проекты» карточки заказчика (шаг 19) — список проектов этого заказчика
   той же выборкой с фильтром по заказчику.
6. Тесты tests/db: подрядчик не получает закрытые проекты даже с status=all в параметрах;
   у сотрудника в результате нет полей бюджета; удалённый проект не попадает никому;
   выгрузка сотрудника отклоняется.

КРИТЕРИИ ПРИЁМКИ
- Критерии 11, 14 (реестр), 17 из docs/ТЗ.md, п. 6.12 проверены в браузере на проектах,
  созданных в базе вручную или сидом для проверки.
- Ответ страницы реестра для сотрудника и подрядчика не содержит сумм бюджета
  (проверено в HTML ответа, а не только на экране).
- Проверено на 1280, 768 и 360 px: таблица прокручивается внутри блока.
- npm run lint, npm run typecheck, npm test проходят.

ЧЕГО НЕ ДЕЛАТЬ
- Не фильтруй бюджет в компоненте вместо выборки.
- Не добавляй колонки расходов, фактических часов и итогов — это этапы 4–5.
- Не добавляй сохранённые представления и выбор колонок — этап 7.
```

---

## Шаг 22. Форма проекта

```
ЦЕЛЬ
Администратор и менеджер создают и правят проекты «В работе».

ИСТОЧНИК
docs/ТЗ.md, пп. 6.6–6.7; docs/СХЕМА-БД.md, 9.3; docs/АРХИТЕКТУРА.md, пп. 3.8–3.9.

ЧТО СДЕЛАТЬ
1. src/features/projects/queries.ts — suggestProjectNumber(actor): наибольший номер
   текущего года (по Europe/Kyiv) среди всех проектов, включая удалённые, и функция шага 17.
2. src/features/projects/actions.ts — createProject и updateProject через authorizedAction:
   - право → схема Zod → транзакция с записью и logAudit;
   - заказчик должен быть активным при создании и при смене заказчика; тот же архивный
     заказчик при правке допустим;
   - нарушение уникального индекса номера → ошибка поля number «Проект с таким номером
     уже есть»;
   - updateProject пишет через updateMany с условием status = IN_PROGRESS и
     deletedAt = null; ноль строк → { ok: false, error: "projects.errors.closed" };
   - сохранение без изменений ничего не пишет; revalidatePath реестра и карточки.
3. src/features/projects/audit.ts — снимок с читаемыми значениями по СХЕМА-БД, 9.4:
   заказчик названием, адрес одной строкой, дата, суммы и часы в формате приложения,
   ставка названием; summary без сумм.
4. Формы /projects/new и /projects/[id]/edit на react-hook-form с той же схемой:
   предложенный номер подставлен и редактируется; выбор заказчика с поиском по активным
   (архивный выбранный — с пометкой); адрес объекта полями как в «Нашей компании»
   (переиспользуй компонент полей адреса); сумма с НДС пересчитывается при вводе;
   ставка НДС по умолчанию 21 %; блокировка «Сохранить» на время отправки; ошибки сервера
   под полями. Страница правки закрытого проекта — сообщение «Проект закрыт — изменения
   вносить нельзя» со ссылкой на карточку, без формы.
5. Тесты tests/db: создание от имени сотрудника отклоняется до валидации; архивный
   заказчик при создании отклоняется, при правке старого проекта — нет; занятый номер —
   ошибка поля, номер удалённого проекта можно занять; правка закрытого проекта
   отклоняется и не пишет журнал; UPDATE содержит только изменённые поля; бюджет пишется
   Decimal без потери центов.

КРИТЕРИИ ПРИЁМКИ
- Критерии 7, 8, 9 и часть 12 (правка закрытого проекта прямым вызовом) из docs/ТЗ.md,
  п. 6.12 проверены.
- Одновременное создание двух проектов с одним предложенным номером даёт второму ошибку
  поля, а не сбой (проверено тестом).
- Форма на 360 px идёт в одну колонку, кнопки видны.
- npm run lint, npm run typecheck, npm test проходят.

ЧЕГО НЕ ДЕЛАТЬ
- Не резервируй номер и не заводи таблицу-счётчик.
- Не проверяй статус отдельным чтением вместо условия записи.
- Не добавляй поля назначенных подрядчиков, ответственного, плановой даты окончания:
  их нет в ТЗ этапа 3.
```

---

## Шаг 23. Карточка проекта, статус, удаление и история

```
ЦЕЛЬ
Карточка проекта со сведениями по ролям, закрытием и возвратом в работу, удалением
и историей изменений.

ИСТОЧНИК
docs/ТЗ.md, пп. 6.7, 6.9, 6.10; docs/СХЕМА-БД.md, 9.3–9.4; docs/ПРАВА-ДОСТУПА.md,
правила 10–12.

ЧТО СДЕЛАТЬ
1. getProject(actor, id): подрядчику — только проект «В работе» с полями подрядчика,
   иначе notFound(); остальным — без удалённых; поля бюджета — только при праве.
2. Карточка /projects/[id]: шапка «Проект <номер>» и название, метка статуса,
   RecordStamps, «Закрыт: дата, автор»; кнопки по правам и статусу. Вкладка «Сведения» —
   строки «название — значение» (тот же компонент, что в «Нашей компании» и справочниках),
   заказчик ссылкой с пометкой «в архиве», длительность. Вкладка «История изменений» —
   при projects.history; у подрядчика вкладок нет.
3. actions.ts — changeProjectStatus(id, target) и deleteProject(id):
   - закрытие: updateMany с условием status = IN_PROGRESS, заполняет closedAt и closedById;
     возврат: условие status = CLOSED, очищает их; ноль строк — «Статус проекта уже изменён»,
     журнал не пишется; STATUS_CHANGE с полем status «В работе» → «Закрыт»;
   - удаление: только projects.delete, условие status = IN_PROGRESS и deletedAt = null,
     DELETE в журнал; после удаления — переход на реестр.
   Диалоги подтверждения с текстами ТЗ п. 6.7 и номером проекта.
4. Бюджет в истории: listEntityAuditLogs и журнал /audit для пользователя без
   projects.budget.read заменяют изменения budgetAmount, vatRate, budgetHours одной
   строкой «Бюджет: изменён» (константа полей бюджета рядом с выборкой журнала,
   СХЕМА-БД, 9.4). Проверь, что значения не уходят в HTML ответа.
5. Тесты tests/db: закрытие и возврат пишут STATUS_CHANGE и заполняют/очищают closedAt;
   повторное закрытие ничего не пишет; менеджер не может удалить; закрытый проект
   не удаляется; сотрудник получает историю без значений бюджета; подрядчик получает
   notFound для закрытого проекта.

КРИТЕРИИ ПРИЁМКИ
- Критерии 10, 12, 13, 14 (карточка), 15 и 16 из docs/ТЗ.md, п. 6.12 проверены в браузере
  под администратором, менеджером, сотрудником и подрядчиком.
- Закрытие в одной вкладке и сохранение формы правки в другой дают сообщение о закрытом
  проекте, а не перезапись.
- Проверено на 1280, 768 и 360 px.
- npm run lint, npm run typecheck, npm test проходят.

ЧЕГО НЕ ДЕЛАТЬ
- Не добавляй восстановление удалённого проекта и корзину.
- Не добавляй третий статус и переходы по датам: статусов два (ТЗ п. 6.7).
```

---

## Шаг 24. Сквозные тесты этапа 3

```
ЦЕЛЬ
Закрыть сквозными тестами доступ по ролям и основные сценарии проекта.

ЧТО СДЕЛАТЬ
1. Playwright (Chromium и WebKit, как на шагах 9 и 15):
   - менеджер создаёт заказчика-компанию, затем проект с этим заказчиком, предложенным
     номером и бюджетом; видит проект в реестре и сумму с НДС в карточке;
   - менеджер закрывает проект: кнопка «Редактировать» исчезает, /projects/[id]/edit
     показывает сообщение о закрытом проекте; возврат в работу возвращает правку;
   - сотрудник видит проект без бюджета и без кнопок правки и выгрузки;
   - подрядчик видит только проекты «В работе», закрытый проект по ссылке — «Не найдено»,
     /customers — «Доступ запрещён»;
   - заказчик в архиве не предлагается в форме нового проекта;
   - администратор привязывает учётную запись подрядчика к организации и видит её
     на вкладке «Учётные записи».
2. На мобильном профиле 360 px: реестр проектов и форма проекта без горизонтальной
   прокрутки страницы (scrollWidth документа не больше ширины окна).
3. Данные каждого теста создаются самим тестом с уникальными номерами и названиями;
   тесты не зависят от порядка и идут параллельно.

КРИТЕРИИ ПРИЁМКИ
- npm test и npm run test:e2e проходят на чистой базе дважды подряд.
- Тесты не обращаются к базе разработчика.

ЧЕГО НЕ ДЕЛАТЬ
- Не повторяй в e2e проверки форматов и расчётов, покрытые модульными тестами шага 17.
```

---

## Шаг 25. Приёмка этапа 3

```
ЦЕЛЬ
Убедиться, что этап 3 завершён, и привести документацию в порядок.

ЧТО СДЕЛАТЬ
1. Пройди чек-лист docs/ТЗ.md, п. 6.12 — все 20 пунктов, вручную в браузере, под
   администратором, менеджером, сотрудником и подрядчиком. Отчёт: пункт — результат —
   замечание.
2. Исправь найденные несоответствия.
3. Проверь, что в коде нет русских строк вне messages/ru.json и что поля бюджета
   не попадают в ответы страниц сотрудника и подрядчика.
4. Обнови README: этап 3 в описании готового, роли, структура.
5. Убедись, что npm run lint, npm run typecheck, npm test, npm run test:e2e
   и npm run format:check проходят.
6. Дополни технический долг в docs/РОАДМАП.md решениями, отложенными на этапе 3;
   сверь вопросы к этапу 4 с тем, что выяснилось при работе.

КРИТЕРИИ ПРИЁМКИ
- Все пункты чек-листа пройдены или по каждому непройденному есть объяснение и план.
- Документы docs/ соответствуют реализованному.

ЧЕГО НЕ ДЕЛАТЬ
- Не начинай этап 4, пока этап 3 не принят.
```

---

# Этап 4. Отчёты о работе

Шаги 26–35. Требования — `docs/ТЗ.md`, раздел 7, и правки пп. 4.11, 6.5–6.8; модели —
`docs/СХЕМА-БД.md`, раздел 10; права — `docs/ПРАВА-ДОСТУПА.md`, строки `reports.*`,
`projects.participants` и правила 16–22; соглашения — `docs/АРХИТЕКТУРА.md`, пп. 3.10–3.12 и 5.

Порядок выбран так, чтобы каждый шаг проверялся: сначала схема и чистые функции, затем права
и пользователи, правила проекта, форма отчёта, реестр с утверждением, итоги в проекте,
выгрузка, импорт и в конце — сквозные тесты и приёмка.

---

## Шаг 26. Схема данных и правила отчёта

```
Продолжаем разработку ConstructionProjects. Этап 3 принят, начинаем этап 4 — отчёты
о работе. Прочитай CLAUDE.md, docs/ТЗ.md (раздел 7 и пп. 4.11, 6.5–6.8), docs/СХЕМА-БД.md
(раздел 10) и docs/АРХИТЕКТУРА.md (пп. 3.10–3.12).

ЦЕЛЬ
Модель отчёта и никнейм пользователя созданы, ограничения базы работают и покрыты тестом,
схемы Zod и расчёты времени покрыты модульными тестами. Интерфейса на этом шаге нет.

ЧТО СДЕЛАТЬ
1. Опиши в prisma/schema.prisma перечисление WorkReportStatus, модель WorkReport, поле
   User.nickname и обратные связи ровно по docs/СХЕМА-БД.md, 10.1–10.2.
2. До миграции найди в базе разработчика проекты «В работе» с одинаковым без учёта регистра
   названием. Если они есть — остановись и покажи список: переименовывать их без моего
   согласия нельзя.
3. Создай миграцию с --create-only (имя work_reports) и допиши SQL из СХЕМА-БД, 10.3.
   Никнейм: колонка добавляется необязательной, блок DO заполняет её у существующих
   пользователей по правилу ТЗ п. 7.4 в порядке createdAt, затем колонка становится
   обязательной и создаётся индекс. Примени миграцию. Покажи дописанный SQL в отчёте
   и никнеймы, которые получили пользователи базы разработчика.
4. src/features/users/nickname.ts — никнейм по умолчанию из ФИО и логина: имя (второе слово
   ФИО, при одном слове — оно), затем «Имя Ф.», «Имя Ф. 2», …; логин, если имя не подходит под
   формат. Занятость передаётся функцией-параметром, сама функция к базе не обращается.
   Схема Zod никнейма (ТЗ п. 7.4) — в src/features/users/schemas.ts.
5. src/features/reports/time.ts: разбор «ЧЧ:ММ» в минуты, минуты → «ЧЧ:ММ», отработанные
   минуты, минуты → десятичные часы «7,50» с округлением «половина вверх» через функции
   src/lib/format.ts — без арифметики с плавающей точкой при округлении.
6. src/features/reports/schemas.ts — схема формы отчёта по ТЗ пп. 7.5–7.6: две версии —
   свой отчёт (поля работника нет, лишний ключ отклоняется) и отчёт за работника (userId
   обязателен); дата не позже сегодняшней по Europe/Kyiv (сравнение с датой начала проекта —
   на сервере, шаг 29); схема причины снятия утверждения (3–500 символов).
7. src/features/reports/overlap.ts — чистая функция пересечений: строки между собой и строки
   с уже внесёнными отчётами, интервалы полуоткрытые [начало, окончание).
8. tests/db/manual-constraints.test.ts: каждое новое ограничение — запись-нарушение,
   отклоняемая именно им, и допустимая запись. Для WorkReport_no_overlap: пересекающийся
   отчёт отклоняется, стыкующийся и отчёт другого работника проходят, удалённый освобождает
   интервал. Для Project_name_in_progress_key: «Дом» и «дом» в работе — нельзя, закрытый
   и удалённый с тем же названием — можно. Сверка списка дополняется исключающими
   ограничениями (contype = 'x').
9. Модульные тесты: «Иванов Иван Иванович» → «Иван», при занятом → «Иван И.», затем «Иван И. 2»;
   одно слово; имя из одной буквы → логин; 08:00–16:30 с обедом 30 → 8,00; 08:00–15:05 → 7,08;
   сумма 20 + 20 мин → 0,67; окончание не позже начала, 24:00, обед не меньше рабочего
   времени, пробег 2001 и дата завтра отклоняются; пересечения, стык, повтор строки.

КРИТЕРИИ ПРИЁМКИ
- npx prisma migrate dev проходит на чистой базе и на базе разработчика; у всех пользователей
  есть никнейм, никнеймы уникальны без учёта регистра.
- База отклоняет пересекающийся отчёт одного работника за день, отчёт с окончанием раньше
  начала, утверждённый отчёт без approvedAt, два никнейма «Иван» и «иван».
- npm test, npm run lint и npm run typecheck — без ошибок; в схемах Zod нет русских строк.

ЧЕГО НЕ ДЕЛАТЬ
- Не создавай страниц, компонентов и server actions — это шаги 27–33.
- Не редактируй применённые миграции этапов 1–3.
- Не храни отработанные часы в базе и не вводи Float для времени и часов.
```

---

## Шаг 27. Права, навигация, никнейм и организация подрядчика

```
ЦЕЛЬ
Раздел «Отчёты» появляется в меню у всех ролей; у пользователя есть никнейм в форме,
карточке и реестре; подрядчика нельзя сохранить без организации.

ИСТОЧНИК
docs/ТЗ.md, пп. 4.5, 6.5, 7.2–7.4; docs/ПРАВА-ДОСТУПА.md — строки этапа 4 и правила 16–22.

ЧТО СДЕЛАТЬ
1. Добавь коды прав этапа 4 в src/lib/permissions.ts ровно по матрице ПРАВА-ДОСТУПА;
   администратору — списком REPORTS_MANAGE, а не "reports.*" (раздел 4 документа).
   Модульный тест матрицы дополни строками этапа 4.
2. Пункт меню «Отчёты» после «Проектов», активная карточка «Отчёты» на главной.
   Страница /reports открывается при reports.read или reports.readOwn и пока показывает
   пустое состояние реестра и кнопку «Новый отчёт» по правам. Подрядчик без организации
   или с архивной организацией видит вместо кнопки сообщение из ТЗ п. 7.3.
3. Никнейм в форме пользователя: при создании поле заполняется из ФИО, пока его не изменили
   вручную; сервер для подставленного значения выбирает первый свободный вариант
   (features/users/nickname.ts), для введённого вручную — возвращает ошибку «Такой никнейм
   уже занят» под полем. Никнейм — в карточке, в профиле (правят администратор и менеджер),
   колонкой и в поиске реестра пользователей, в выгрузке реестра. Правка пишется в аудит
   (UPDATE, поле nickname).
4. Организация обязательна для роли «Подрядчик» в схеме и в действии: создание и правка
   подрядчика без организации отклоняются ошибкой под полем.
5. "WorkReport" — в AUDIT_ENTITIES с названием «Отчёт о работе».
6. Тесты с базой: менеджер не меняет никнейм администратора прямым вызовом; занятый никнейм
   в другом регистре — ошибка поля; подстановка выбирает «Иван И.», когда «Иван» занят;
   подрядчик без организации не сохраняется.

КРИТЕРИИ ПРИЁМКИ
- Пункт «Отчёты» и карточка на главной видны всем четырём ролям; /reports открывается у всех.
- Никнейм правится, занятый даёт ошибку под полем; реестр ищет по никнейму.
- Форма подрядчика без организации не сохраняется ни в интерфейсе, ни прямым вызовом.
- npm run lint, npm run typecheck, npm test проходят; экраны пользователей проверены
  в браузере на 1280, 768 и 360 px.

ЧЕГО НЕ ДЕЛАТЬ
- Не делай формы отчёта, реестра и карточки отчёта — шаги 29–30.
- Не давай менеджеру поле «Организация»: его по-прежнему меняет только администратор.
```

---

## Шаг 28. Правила проекта для отчётов и поля с отдельным правом

```
ЦЕЛЬ
Проект не закрывается с неутверждёнными отчётами, не удаляется с отчётами, его название
«В работе» уникально; скрытие бюджета в истории держится на пометках полей, а не на списке.

ИСТОЧНИК
docs/ТЗ.md, пп. 6.6–6.7 и 7.8; docs/СХЕМА-БД.md, 10.3–10.4; docs/АРХИТЕКТУРА.md, пп. 3.10, 3.12.

ЧТО СДЕЛАТЬ
1. Уникальное название: нарушение Project_name_in_progress_key при создании и правке —
   ошибка под полем «Название»; возврат в работу с занятым названием отклоняется сообщением
   из ТЗ п. 6.7.
2. Закрытие проекта: в одной транзакции сначала обновление строки проекта, затем подсчёт
   неутверждённых отчётов; больше нуля — откат и сообщение с их числом (ICU plural).
3. Удаление проекта отклоняется, если у него есть отчёты, включая удалённые.
4. Дата начала проекта не может стать позже его первого неудалённого отчёта — ошибка под полем.
5. Замени PROJECT_BUDGET_FIELDS пометками в описании полей сущности для журнала
   (АРХИТЕКТУРА п. 3.12); поведение истории и журнала не меняется. Модульный тест проходит
   по полям моделей Prisma (Prisma.dmmf) и падает на поле Decimal без пометки.
6. Тесты с базой — отчёты создаются в тесте напрямую через Prisma: закрытие с неутверждённым
   отклоняется и называет число, с утверждёнными проходит; отчёт, вставленный в другой
   транзакции одновременно с закрытием, либо учитывается, либо отклоняется (два соединения,
   порядок задаётся явно); удаление проекта с удалённым отчётом отклоняется; «Дом» / «дом».

КРИТЕРИИ ПРИЁМКИ
- Все правила выполняются и в интерфейсе карточки проекта, и прямым вызовом действий.
- Тесты шагов 21–24 проходят без правок ожиданий, кроме тех, что меняет ТЗ 1.5.
- npm run lint, npm run typecheck, npm test проходят.

ЧЕГО НЕ ДЕЛАТЬ
- Не делай интерфейса отчётов — шаги 29–31.
- Не проверяй статус проекта отдельным чтением перед записью вместо блокировки.
```

---

## Шаг 29. Форма отчёта: внесение, правка, удаление

```
ЦЕЛЬ
Сотрудник и подрядчик вносят свои отчёты с телефона, менеджер — за работника; правка
и удаление работают по правилам статусов, закрытого проекта и архива подрядчика.

ИСТОЧНИК
docs/ТЗ.md, пп. 7.2–7.8; docs/ПРАВА-ДОСТУПА.md, правила 16–19; docs/АРХИТЕКТУРА.md, п. 3.10.

ЧТО СДЕЛАТЬ
1. src/features/reports/actions.ts: createOwnReport (reports.writeOwn), createReport
   (reports.write, с работником), updateReport, deleteReport — каждое: права → Zod →
   транзакция по АРХИТЕКТУРА п. 3.10 (строка проекта FOR SHARE, запись с условием статуса
   отчёта, аудит). На сервере: дата не раньше начала проекта; работник и его организация
   (ТЗ пп. 7.2, 7.8); contractorId — снимок организации работника; работник после создания
   не меняется. Ошибка 23P01 → ошибка поля «Начало работ» со ссылкой на мешающий отчёт.
2. src/features/reports/audit.ts — читаемые значения changes и summary по СХЕМА-БД, 10.5.
3. Страницы /reports/new (параметр ?project= выбирает проект) и /reports/[id]/edit: поля
   ТЗ п. 7.5, «Отработано, ч» пересчитывается при вводе, выбор работника (ADMIN, MANAGER)
   и проекта с поиском, предупреждение о несохранённых изменениях, после сохранения — переход
   в /reports с сообщением. Форма в одну колонку до 768 px, поля даты и времени — стандартные.
4. Тесты с базой — прямым вызовом действий: чужой userId от подрядчика, подрядчик без
   организации и с архивной организацией, неактивный работник, администратор как работник,
   закрытый проект, дата в будущем и раньше начала проекта, пересечение и стык, правка
   утверждённого своего отчёта, удаление утверждённого менеджером, менеджер правит
   утверждённый — статус сохраняется, аудит с читаемыми значениями.

КРИТЕРИИ ПРИЁМКИ
- Подрядчик на 360 px вносит отчёт без горизонтальной прокрутки и видит часы сразу.
- Менеджер вносит отчёт за работника; «Создано» в журнале — менеджер.
- Все отказы из п. 4 срабатывают прямым вызовом, а не только скрытием в интерфейсе.
- npm run lint, npm run typecheck, npm test проходят; сценарий проверен в браузере
  на 1280, 768 и 360 px под подрядчиком и менеджером.

ЧЕГО НЕ ДЕЛАТЬ
- Не делай реестра с фильтрами, карточки отчёта и утверждения — шаг 30.
- Не проверяй пересечения запросом перед записью: их отклоняет ограничение базы.
- Не добавляй файлы и фото к отчёту.
```

---

## Шаг 30. Реестр отчётов, карточка и утверждение

```
ЦЕЛЬ
Реестр «Отчёты» с фильтрами и итогами по ролям, карточка отчёта с историей, утверждение
одного и нескольких отчётов и снятие утверждения с причиной.

ИСТОЧНИК
docs/ТЗ.md, пп. 7.7, 7.9–7.10, 7.14; docs/ПРАВА-ДОСТУПА.md, правила 16, 18, 21.

ЧТО СДЕЛАТЬ
1. src/features/reports/queries.ts: одна функция условия от прав и фильтров (без reports.read —
   только свои отчёты) для реестра, итогов и выборки одного отчёта; чужой или удалённый
   отчёт — «Не найдено». Итоги выборки — агрегатом в SQL по всем страницам.
2. /reports: колонки, фильтры, поиск, сортировка и итоги ТЗ п. 7.9 по ролям; по умолчанию
   ADMIN и MANAGER видят неутверждённые; флажки строк и «Утвердить выбранные»; строчные
   действия; значок снятого утверждения с причиной в подсказке.
3. /reports/[id]: шапка, «Сведения», «История изменений» (reports.history) по ТЗ п. 7.10;
   кнопки «Редактировать», «Удалить» (с подтверждением), «Утвердить», «Снять утверждение»
   (диалог с причиной) — по правам, статусу отчёта и статусу проекта.
4. Действия approveReport, approveReports (массовое, сообщение «Утверждено / пропущено»),
   unapproveReport — права → Zod → транзакция со строкой проекта FOR SHARE и условием статуса
   отчёта → аудит STATUS_CHANGE на каждый отчёт. Повторное действие — «Статус отчёта уже изменён».
5. Тесты с базой: работник подрядчика не видит отчёт коллеги по организации ни в реестре,
   ни по id; итоги считают все страницы; массовое утверждение пропускает утверждённые
   и отчёты закрытого проекта; снятие без причины отклоняется; работник правит отчёт после
   снятия утверждения.

КРИТЕРИИ ПРИЁМКИ
- Состояние реестра восстанавливается из адресной строки; итоги совпадают с суммой строк.
- Утверждение и снятие работают из карточки, массовое — из реестра; всё пишется в журнал.
- npm run lint, npm run typecheck, npm test проходят; реестр и карточка проверены в браузере
  под всеми ролями на 1280, 768 и 360 px.

ЧЕГО НЕ ДЕЛАТЬ
- Не делай вкладок проекта, выгрузки и импорта — шаги 31–33.
- Не фильтруй чужие отчёты в компоненте: только условием выборки.
```

---

## Шаг 31. Отчёты, итоги и участники в карточке проекта

```
ЦЕЛЬ
В карточке проекта появляются вкладки «Отчёты» с итогами и сравнением с бюджетом часов
и «Участники».

ИСТОЧНИК
docs/ТЗ.md, пп. 7.3, 7.11; docs/ПРАВА-ДОСТУПА.md, правило 20.

ЧТО СДЕЛАТЬ
1. Вкладки карточки проекта в порядке «Сведения», «Отчёты», «Участники», «История изменений».
   «Отчёты» — таблица реестра шага 30 с проектом, заданным страницей; кнопка «Новый отчёт»
   (проект «В работе») ведёт на /reports/new?project=….
2. Итоги проекта у ADMIN и MANAGER: часы и км утверждённые и всего; при projects.budget.read
   и заданном бюджете часов — «использовано N %» или «Превышен на X ч». Сотрудник и подрядчик
   видят итоги только своих отчётов, без бюджета. Проценты — из минут, без Float.
3. «Участники» (projects.participants): группы по организации отчёта, итог группы, колонки
   ТЗ п. 7.11. Выборка отдаёт их только при праве; адрес вкладки без права — «Доступ запрещён».
4. Тесты с базой: итоги с утверждёнными и неутверждёнными, удалённые не считаются; превышение
   бюджета; участник числится в организации отчёта после смены привязки; бюджет часов не
   читается выборкой без права.

КРИТЕРИИ ПРИЁМКИ
- Итоги совпадают с суммой отчётов проекта; сравнение с бюджетом — только по утверждённым.
- Подрядчик видит на вкладке только свои отчёты и не видит «Участников».
- npm run lint, npm run typecheck, npm test проходят; карточка проверена в браузере на
  1280, 768 и 360 px.

ЧЕГО НЕ ДЕЛАТЬ
- Не добавляй деньги за часы и пробег — это этап 5.
- Не показывай бюджет часов без projects.budget.read даже процентом.
```

---

## Шаг 32. Выгрузка табеля и лимит строк PDF и печати

```
ЦЕЛЬ
Табель выгружается в Excel, PDF и на печать с итоговой строкой; PDF и печать всех реестров
ограничены 2 000 строк.

ИСТОЧНИК
docs/ТЗ.md, пп. 4.11, 6.8, 7.12; docs/АРХИТЕКТУРА.md, п. 5.

ЧТО СДЕЛАТЬ
1. src/lib/export: итоговая строка (totals) во всех трёх форматах; лимит 2 000 строк для PDF
   и печати в общем модуле — подсчёт строк до выборки, неактивные кнопки с подсказкой,
   400 у маршрута PDF, сообщение в печатном представлении. Excel — без лимита.
2. src/features/reports/export.ts — отчёт «Табель отчётов о работе» (reports.export):
   колонки ТЗ п. 7.12, итоги часов и км считает выборка в SQL, название перечисляет фильтры.
   Кнопки — в реестре /reports и на вкладке «Отчёты» проекта (с фильтром проекта).
3. Выгрузка пишет EXPORT с числом строк, как остальные отчёты.
4. Тесты: модульные — итоговая строка в xlsx (числами) и в PDF; с базой — 2 001 строка
   блокирует PDF и печать, но не Excel (данные — createMany в тестовой базе); выгрузка табеля
   сотрудником отклоняется.

КРИТЕРИИ ПРИЁМКИ
- Табель учитывает фильтры, строка «Итого» совпадает с итогами реестра.
- Лимит действует в реестрах пользователей, журнала, заказчиков, подрядчиков, проектов
  и отчётов без кода в каждом из них.
- npm run lint, npm run typecheck, npm test проходят; файлы открыты и проверены глазами.

ЧЕГО НЕ ДЕЛАТЬ
- Не делай табель таблицей «дни × работники» (ТЗ п. 7.15).
- Не вводи лимит для Excel.
```

---

## Шаг 33. Импорт отчётов из Excel

```
ЦЕЛЬ
Общий механизм импорта и импорт отчётов: шаблон, предпросмотр с ошибками по строкам,
подтверждение и запись целиком.

ИСТОЧНИК
docs/ТЗ.md, п. 7.13; docs/АРХИТЕКТУРА.md, п. 3.11; docs/СХЕМА-БД.md, 10.5.

ЧТО СДЕЛАТЬ
1. src/lib/import по АРХИТЕКТУРА п. 3.11: разбор первого листа ExcelJS, поиск колонок
   по заголовкам, приведение ячеек-дат, ячеек-времени и текста, пропуск пустых строк, номера
   строк файла; ошибки файла целиком; шаблон с листами данных и «Инструкция»; действия
   «проверить» и «подтвердить» с повторной проверкой; лимиты 5 МБ и 2 000 строк,
   bodySizeLimit в next.config.ts.
2. src/features/reports/import.ts — описание импорта отчётов: колонки шаблона, проверка строк
   (правила формы, никнейм, работник и организация, проект «В работе» по названию,
   пересечения с базой и внутри файла через overlap.ts), запись createMany в транзакции
   со строками проектов FOR SHARE, аудит CREATE на отчёт и одна запись IMPORT.
3. /reports/import (reports.import): «Скачать шаблон», выбор файла, предпросмотр ТЗ п. 7.13
   со сводкой и переключателем «Только ошибки», флажок «Сразу утвердить», кнопка с числом
   отчётов; при изменившихся данных — новый предпросмотр с сообщением.
4. Тесты: модульные — разбор ячеек и текста, пустые строки, нет колонки, 2 001 строка;
   с базой — файл с ошибкой ничего не пишет; файл без ошибок пишется целиком, со «Сразу
   утвердить» — утверждённым; повторная загрузка даёт ошибки пересечения; проект закрыт между
   предпросмотром и подтверждением — ничего не записано; сотрудник не импортирует.
   Файлы для тестов строятся в самих тестах через ExcelJS.

КРИТЕРИИ ПРИЁМКИ
- Шаблон, заполненный по инструкции, импортируется без ошибок.
- Ни одна ошибка не приводит к частичной записи.
- npm run lint, npm run typecheck, npm test проходят; сценарий проверен в браузере.

ЧЕГО НЕ ДЕЛАТЬ
- Не заводи таблицу-черновик импорта и не храни файл на сервере.
- Не импортируй другие сущности и не обновляй существующие отчёты — это этап 6.
```

---

## Шаг 34. Сквозные тесты этапа 4 и проверка утечки бюджета

```
ЦЕЛЬ
Сквозные тесты сценариев отчётов по ролям и автоматическая проверка, что бюджет не уходит
в ответы страниц сотрудника и подрядчика на продакшн-сборке.

ЧТО СДЕЛАТЬ
1. Playwright (Chromium и WebKit, как на шагах 15 и 24):
   - подрядчик на 360 px вносит отчёт и видит его «Не утверждён»; коллега по организации
     его не видит;
   - менеджер утверждает отчёты «Утвердить выбранные»; подрядчик не может править
     утверждённый; менеджер снимает утверждение с причиной — подрядчик видит причину и правит;
   - закрытие проекта с неутверждённым отчётом отклоняется с их числом;
   - менеджер импортирует файл (строится в тесте ExcelJS) со «Сразу утвердить»; файл
     с ошибкой показывает ошибку строки и не импортируется;
   - менеджер выгружает табель в Excel и находит строку «Итого»; видит вкладку «Участники»,
     сотрудник её не видит.
2. На 360 px: реестр отчётов, форма и карточка отчёта без горизонтальной прокрутки.
3. Проверка утечки бюджета (решение 18.09.2026, РОАДМАП): отдельный скрипт npm run test:prod —
   собирает приложение (next build в отдельный каталог), запускает next start на своём порту
   с e2e-базой, входит сотрудником и подрядчиком, запрашивает страницы проектов и отчётов
   (HTML и RSC-запросы) и проверяет, что в ответах нет значений бюджета проекта с заведомо
   уникальными суммами. На next dev эта проверка недостоверна: dev кладёт данные запросов
   в поток страницы.
4. Данные каждого теста создаются самим тестом с уникальными названиями, никнеймами и
   номерами; тесты не зависят от порядка и идут параллельно.

КРИТЕРИИ ПРИЁМКИ
- npm test, npm run test:e2e и npm run test:prod проходят на чистой базе дважды подряд.
- Тесты не обращаются к базе разработчика.

ЧЕГО НЕ ДЕЛАТЬ
- Не повторяй в e2e проверки, покрытые модульными тестами и тестами с базой шагов 26–33.
```

---

## Шаг 35. Приёмка этапа 4

```
ЦЕЛЬ
Убедиться, что этап 4 завершён, замерить реестры на больших данных и привести документацию
в порядок.

ЧТО СДЕЛАТЬ
1. Пройди чек-лист docs/ТЗ.md, п. 7.16 — все пункты, вручную в браузере, под администратором,
   менеджером, сотрудником и подрядчиком. Отчёт: пункт — результат — замечание.
2. Исправь найденные несоответствия.
3. Замер (решение 18.09.2026, РОАДМАП): на отдельной базе, не на базе разработчика, сгенерируй
   100 000 отчётов, 100 000 записей журнала, 10 000 проектов и замерь открытие реестров
   отчётов, журнала и проектов с поиском и фильтрами (ТЗ п. 3.2 — до 1 с). Где поиск по
   подстроке медленнее — миграция с pg_trgm и триграммными индексами; повторный замер.
   Результаты — в отчёт и в строку техдолга РОАДМАП.
4. Проверь, что в коде нет русских строк вне messages/ru.json.
5. Обнови README: этап 4 в описании готового, роли, импорт, npm run test:prod, структура.
6. Убедись, что npm run lint, npm run typecheck, npm test, npm run test:e2e, npm run test:prod
   и npm run format:check проходят.
7. Дополни технический долг в docs/РОАДМАП.md решениями, отложенными на этапе 4;
   сверь вопросы к этапу 5 с тем, что выяснилось при работе.

КРИТЕРИИ ПРИЁМКИ
- Все пункты чек-листа пройдены или по каждому непройденному есть объяснение и план.
- Реестры укладываются в 1 с на 100 000 записей или по каждому есть объяснение и план.
- Документы docs/ соответствуют реализованному.

ЧЕГО НЕ ДЕЛАТЬ
- Не начинай этап 5, пока этап 4 не принят.
```

---

## Вспомогательные промты

Применяются по необходимости, между основными шагами.

### Ревью перед приёмкой шага

```
Проверь изменения последнего шага на соответствие docs/ТЗ.md,
docs/ПРАВА-ДОСТУПА.md и правилам из CLAUDE.md. Обрати внимание:
- есть ли server action без проверки прав или без валидации Zod;
- есть ли изменение данных без записи в аудит;
- есть ли русские строки вне messages/ru.json;
- есть ли выборки без исключения мягко удалённых записей;
- есть ли обращения к Prisma в обход queries.ts и actions.ts.
Выдай список найденного с указанием файла и строки, отсортированный
по серьёзности. Не исправляй ничего без моего подтверждения.
```

### Добавление поля в существующую сущность

```
Добавь поле <название> (<тип>, <обязательность>, <правила валидации>)
в сущность <модель>. Нужно пройти всю цепочку:
схема Prisma и миграция → схема Zod → форма → колонка реестра (если требуется)
→ карточка → описание колонок для выгрузки → переводы в messages/ru.json
→ фиксация в docs/СХЕМА-БД.md и docs/ТЗ.md.
Проверь, что поле корректно попадает в журнал изменений.
```

### Разбор ошибки

```
Опиши, что происходит: <симптом>. Не предлагай правку, пока не найдёшь причину.
Сначала объясни, почему это происходит, и где именно; затем предложи
минимальное исправление и скажи, чем его проверить.
```

### Начало новой сессии посреди этапа

```
Продолжаем разработку ConstructionProjects. Прочитай CLAUDE.md и docs/ТЗ.md.
Сделано по шаг <N> включительно. Сейчас работаем над шагом <N+1>.
Прежде чем писать код, скажи, что уже есть в проекте по этой теме,
и предложи план в 3–5 пунктов.
```
