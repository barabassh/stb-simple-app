# Права доступа — этапы 1–4

## 1. Главное правило

**Право проверяется на сервере в каждом server action и в каждом запросе данных.**
Скрытая в интерфейсе кнопка — удобство для пользователя, а не защита: любой
запрос можно повторить в обход интерфейса.

Порядок в каждом действии: определить текущего пользователя → проверить право →
проверить входные данные → выполнить. Проверка права идёт **до** валидации:
пользователю без доступа не следует сообщать, что именно не так с данными.

## 2. Матрица «роль × операция»

| Операция | Код права | Администратор | Менеджер | Сотрудник | Подрядчик |
|---|---|:---:|:---:|:---:|:---:|
| Вход в приложение | — | ✅ | ✅ | ✅ | ✅ |
| Просмотр реестра пользователей | `users.read` | ✅ | ✅ | ❌ | ❌ |
| Просмотр карточки пользователя | `users.read` | ✅ | ✅ | ❌ | ❌ |
| Просмотр истории изменений пользователя | `users.history.read` | ✅ | ✅ | ❌ | ❌ |
| Создание пользователя | `users.create` | ✅ | ❌ | ❌ | ❌ |
| Редактирование профиля другого пользователя: ФИО, никнейм, должность, email, телефон (кроме администраторов) | `users.updateProfile` | ✅ | ✅ | ❌ | ❌ |
| Редактирование профиля администратора и комментария к учётной записи | `users.update` | ✅ | ❌ | ❌ | ❌ |
| Смена роли пользователя | `users.changeRole` | ✅ | ❌ | ❌ | ❌ |
| Сброс пароля пользователя | `users.resetPassword` | ✅ | ❌ | ❌ | ❌ |
| Деактивация / активация | `users.changeStatus` | ✅ | ❌ | ❌ | ❌ |
| Просмотр чужих сессий | `users.sessions.read` | ✅ | ❌ | ❌ | ❌ |
| Завершение чужой сессии | `users.sessions.revoke` | ✅ | ❌ | ❌ | ❌ |
| Выгрузка реестра пользователей | `users.export` | ✅ | ✅ | ❌ | ❌ |
| Просмотр журнала аудита | `audit.read` | ✅ | ❌ | ❌ | ❌ |
| Выгрузка журнала аудита | `audit.export` | ✅ | ❌ | ❌ | ❌ |
| Просмотр своего профиля | `profile.read` | ✅ | ✅ | ✅ | ✅ |
| Редактирование своего профиля | `profile.update` | ✅ | ✅ | ❌ | ❌ |
| Просмотр и завершение своих сессий | `profile.sessions` | ✅ | ✅ | ✅ | ✅ |
| Раздел «Настройки»: пункт меню, карточка, страница `/settings` | `settings.read` | ✅ | ✅ | ❌ | ❌ |
| Просмотр сведений о компании | `settings.company.read` | ✅ | ✅ | ❌ | ❌ |
| Заполнение и редактирование сведений о компании | `settings.company.update` | ✅ | ✅ | ❌ | ❌ |
| История изменений сведений о компании | `settings.company.history` | ✅ | ✅ | ❌ | ❌ |
| Привязка учётной записи подрядчика к организации | `users.changeContractor` | ✅ | ❌ | ❌ | ❌ |
| Просмотр реестра и карточек заказчиков | `customers.read` | ✅ | ✅ | ✅ | ❌ |
| Создание заказчика | `customers.create` | ✅ | ✅ | ❌ | ❌ |
| Редактирование заказчика | `customers.update` | ✅ | ✅ | ❌ | ❌ |
| Перевод заказчика в архив и восстановление | `customers.changeStatus` | ✅ | ✅ | ❌ | ❌ |
| Выгрузка реестра заказчиков | `customers.export` | ✅ | ✅ | ❌ | ❌ |
| История изменений заказчика | `customers.history` | ✅ | ✅ | ✅ | ❌ |
| Просмотр реестра и карточек подрядчиков | `contractors.read` | ✅ | ✅ | ✅ | ❌ |
| Создание подрядчика | `contractors.create` | ✅ | ✅ | ❌ | ❌ |
| Редактирование подрядчика | `contractors.update` | ✅ | ✅ | ❌ | ❌ |
| Перевод подрядчика в архив и восстановление | `contractors.changeStatus` | ✅ | ✅ | ❌ | ❌ |
| Выгрузка реестра подрядчиков | `contractors.export` | ✅ | ✅ | ❌ | ❌ |
| История изменений подрядчика | `contractors.history` | ✅ | ✅ | ✅ | ❌ |
| Просмотр всех проектов | `projects.read` | ✅ | ✅ | ✅ | ❌ |
| Просмотр проектов «В работе» без заказчика и бюджета | `projects.readActive` | ✅ | ✅ | ✅ | ✅ |
| Просмотр бюджета проекта | `projects.budget.read` | ✅ | ✅ | ❌ | ❌ |
| Создание проекта | `projects.create` | ✅ | ✅ | ❌ | ❌ |
| Редактирование проекта «В работе» | `projects.update` | ✅ | ✅ | ❌ | ❌ |
| Закрытие проекта и возврат в работу | `projects.changeStatus` | ✅ | ✅ | ❌ | ❌ |
| Удаление проекта «В работе» | `projects.delete` | ✅ | ❌ | ❌ | ❌ |
| Выгрузка реестра проектов | `projects.export` | ✅ | ✅ | ❌ | ❌ |
| История изменений проекта | `projects.history` | ✅ | ✅ | ✅ | ❌ |
| Участники проекта (вкладка «Участники») | `projects.participants` | ✅ | ✅ | ❌ | ❌ |
| Просмотр своих отчётов о работе, итоги своих отчётов | `reports.readOwn` | ❌ | ❌ | ✅ | ✅ |
| Внесение своего отчёта, правка и удаление своего неутверждённого | `reports.writeOwn` | ❌ | ❌ | ✅ | ✅ |
| Просмотр всех отчётов, фильтры и итоги, итоги проекта | `reports.read` | ✅ | ✅ | ❌ | ❌ |
| Внесение отчёта за работника, правка любого, удаление неутверждённого | `reports.write` | ✅ | ✅ | ❌ | ❌ |
| Утверждение отчёта и снятие утверждения | `reports.approve` | ✅ | ✅ | ❌ | ❌ |
| Выгрузка табеля | `reports.export` | ✅ | ✅ | ❌ | ❌ |
| Импорт отчётов из Excel | `reports.import` | ✅ | ✅ | ❌ | ❌ |
| История изменений отчёта | `reports.history` | ✅ | ✅ | ❌ | ❌ |

В своём профиле администратор и менеджер редактируют ФИО, никнейм, должность, email и телефон;
сотрудник и подрядчик свой профиль только просматривают (`ТЗ.md`, п. 4.8). Логин не
меняется вообще (`ТЗ.md`, п. 4.5), роль меняет только администратор в форме пользователя
(`users.changeRole`). Свой пароль пользователь не меняет: пароли назначает и сбрасывает
только администратор (`users.resetPassword`).

## 3. Правила, не выражаемые матрицей

Проверяются отдельно, после проверки права:

1. **Нельзя деактивировать или понизить в роли последнего активного
   администратора.** Проверка выполняется в транзакции — иначе два одновременных
   запроса способны снять обоих оставшихся администраторов.
2. **Нельзя деактивировать самого себя.** Право у администратора есть,
   но действие отклоняется.
3. **Профиль администратора правит только администратор.** Менеджер правит ФИО, никнейм,
   должность, email и телефон остальных пользователей, в том числе других менеджеров;
   роль, статус, комментарий, пароль и сессии других пользователей ему недоступны.
   Нужное право зависит от роли того, чей профиль правится, поэтому действие проверяет
   его после чтения записи: `requirePermission(actor, userUpdatePermission(target))`.
4. **Сотрудник и подрядчик не имеют доступа к `/users` и `/audit`.** Переход по
   прямой ссылке приводит к странице «Доступ запрещён», а не к пустому реестру.
   Свой профиль они только просматривают и завершают в нём свои сессии.
5. **Деактивированный пользователь не входит в приложение**, а его активные сессии
   прекращаются немедленно при деактивации.
6. **Пользователей не удаляет никто.** Операции удаления учётной записи нет ни
   у одной роли — только деактивация (`ТЗ.md`, п. 4.7).
7. **История изменений в карточке не открывает журнал.** Менеджер видит на вкладке
   «История изменений», кто, когда и что изменил, но `/audit` ему недоступен. IP-адрес
   и браузер записей истории отдаются только роли с `audit.read`: чужие сессии
   менеджеру не показываются (`users.sessions.read`), поэтому и в истории их данных нет.
8. **Раздел «Настройки» закрыт для сотрудника и подрядчика целиком.** Переход на
   `/settings` и любой вложенный адрес ведёт на «Доступ запрещён». Права раздела
   разбиты по блокам (`settings.<блок>.*`): следующий блок может получить другой круг
   ролей, не меняя права существующих. Доступ к странице `/settings` (`settings.read`)
   не даёт доступа к данным блока — страница показывает только те блоки, на чтение
   которых у пользователя есть право.
9. **Сведения о компании правит одно право целиком.** Отдельных прав на группы полей
   (реквизиты, адреса, контакты) нет: администратор и менеджер правят все поля блока.
   История изменений блока, как и в карточке пользователя, отдаёт IP-адрес и браузер
   только роли с `audit.read`.
10. **Закрытый проект не изменяется никем.** `projects.update` и `projects.delete` действуют
    только на проекты «В работе». Статус проверяется не отдельным чтением, а условием самой
    записи в транзакции (`СХЕМА-БД.md`, 9.3): правка, отправленная после закрытия,
    отклоняется с ошибкой `projects.errors.closed`. Единственная операция над закрытым
    проектом — возврат в работу (`projects.changeStatus`). Действия этапов 4 и 5, вносящие данные
    в проект, подчиняются тому же правилу.
11. **Подрядчик видит только проекты «В работе» и только часть полей.** Роль с
    `projects.readActive`, но без `projects.read`, получает из выборки проекты со статусом
    «В работе» — условие добавляет сама выборка в `queries.ts`, а не фильтр в URL. Выборка
    возвращает для неё отдельный набор полей без заказчика, бюджета и отметок. Закрытый или
    удалённый проект по прямой ссылке — «Не найдено», а не «Доступ запрещён»: существование
    проекта не раскрывается.
12. **Бюджет — отдельное право.** Без `projects.budget.read` поля бюджета не читаются из базы
    вовсе (а не скрываются в компоненте), не попадают в выгрузку, а изменения бюджета
    в истории проекта показываются без значений. Правка проекта (`projects.update`) выдана
    только ролям, у которых есть и `projects.budget.read`.
13. **Заказчики и подрядчики не удаляются**, их переводят в архив (`*.changeStatus`).
    Архивного заказчика нельзя выбрать при создании проекта или смене заказчика, архивного
    подрядчика — при привязке учётной записи: действие проверяет это на сервере, а не только
    список выбора в форме. Уже сохранённые ссылки на архивную запись остаются.
14. **Привязку учётной записи к подрядчику меняет только администратор**
    (`users.changeContractor`). Менеджер, правящий профиль подрядчика, поля «Организация»
    не видит; попытка передать его в обход формы отклоняется так же, как смена роли.
    Привязка допустима только у роли «Подрядчик» и снимается при смене роли.
15. **Вкладка «Учётные записи» карточки подрядчика** показывается только при `users.read`:
    сотрудник видит подрядчика, но не список учётных записей.
16. **Свои отчёты — условием выборки.** Роль с `reports.readOwn`, но без `reports.read`,
    получает из выборки только отчёты, где работник — она сама: условие `userId` добавляет
    выборка в `queries.ts`, а не фильтр в URL. Чужой или удалённый отчёт по прямой ссылке —
    «Не найдено», а не «Доступ запрещён». Работники одной организации отчётов друг друга
    не видят.
17. **Кто может быть работником.** Действие с `reports.writeOwn` всегда записывает отчёт на
    текущего пользователя и не принимает поле работника; переданный в обход формы `userId`
    отклоняется, как смена роли. Действие с `reports.write` принимает работника и проверяет
    на сервере: пользователь активен, роль — «Сотрудник» или «Подрядчик», у подрядчика есть
    активная организация. Подрядчик без организации или с архивной организацией не вносит
    отчёт и с `reports.writeOwn` (`ТЗ.md`, п. 7.3).
18. **Утверждённый отчёт.** `reports.writeOwn` правит и удаляет только неутверждённый свой
    отчёт — условием записи `status = UNAPPROVED`, а не чтением перед ней. `reports.write`
    правит и утверждённый, но не удаляет его: сначала снимается утверждение (`reports.approve`)
    с обязательной причиной.
19. **Отчёты закрытого проекта не изменяются никем** — продолжение правила 10: внесение,
    правка, удаление, утверждение и снятие утверждения проверяют статус проекта в транзакции
    записи (`СХЕМА-БД.md`, 10.4). Закрытие проекта (`projects.changeStatus`) отклоняется,
    пока в нём есть неутверждённые отчёты; удаление (`projects.delete`) — пока есть отчёты.
20. **Итоги и участники.** Итоги всех отчётов проекта — при `reports.read`, сравнение с бюджетом
    часов — дополнительно при `projects.budget.read`. С `reports.readOwn` итоги считаются только
    по своим отчётам. Участников проекта отдаёт выборка при `projects.participants`;
    подрядчик, знающий адрес вкладки, получает «Доступ запрещён».
21. **Подрядчик и закрытый проект.** Свои отчёты закрытого проекта подрядчик видит в реестре
    и карточке отчёта: номер и название проекта выборка отдаёт как часть отчёта, но карточка
    закрытого проекта ему по-прежнему недоступна (правило 11).
22. **Никнейм** правят по тем же правилам, что ФИО (правило 3): менеджер — у всех, кроме
    администраторов, отдельного права нет.

## 4. Реализация

Права описываются строками и матрицей в `src/lib/permissions.ts`:

```ts
export const PERMISSIONS = {
  ADMIN:      ["users.*", "audit.*", "profile.*", "settings.*",
               "customers.*", "contractors.*", "projects.*", ...REPORTS_MANAGE],
  MANAGER:    ["users.read", "users.history.read", "users.updateProfile", "users.export", "profile.*",
               "settings.read", "settings.company.read", "settings.company.update",
               "settings.company.history", "customers.*", "contractors.*",
               "projects.read", "projects.readActive", "projects.budget.read", "projects.create",
               "projects.update", "projects.changeStatus", "projects.export", "projects.history",
               "projects.participants", ...REPORTS_MANAGE],
  EMPLOYEE:   ["profile.read", "profile.sessions", "customers.read", "customers.history",
               "contractors.read", "contractors.history", "projects.read", "projects.readActive",
               "projects.history", "reports.readOwn", "reports.writeOwn"],
  CONTRACTOR: ["profile.read", "profile.sessions", "projects.readActive",
               "reports.readOwn", "reports.writeOwn"],
} as const;

// Администратор получает права отчётов списком, а не "reports.*": с подстановкой он получил
// бы и reports.readOwn / reports.writeOwn, хотя своих отчётов не вносит (ТЗ.md, п. 7.2).
const REPORTS_MANAGE = ["reports.read", "reports.write", "reports.approve", "reports.export",
                        "reports.import", "reports.history"];

export function can(user: SessionUser, permission: Permission): boolean;
export function requirePermission(user: SessionUser, permission: Permission): void;
// "users.update" для администратора, "users.updateProfile" для остальных
export function userUpdatePermission(target: { role: Role }): Permission;
```

`requirePermission` бросает `PermissionDeniedError`, которая наверху превращается в
страницу «Доступ запрещён» либо в отказ действия — в зависимости от того, пришёл
запрос за страницей или за мутацией:

- **Страница** раздела начинается с `requirePagePermission(permission)`
  (`src/lib/auth/current-user.ts`): пользователь без права перенаправляется на
  `/forbidden` ещё до чтения данных.
- **Server action** объявляется через `authorizedAction(permission, action)`
  (`src/lib/auth/authorized-action.ts`): пользователь и право проверяются до разбора
  аргументов, отказ возвращается результатом `{ ok: false, error: "errors.forbiddenAction" }`.
  Права, зависящие от данных (правка администратора, смена роли, статуса и комментария
  в форме редактирования), проверяются внутри действия тем же `requirePermission`
  и дают тот же отказ.
- **Выборка** в `queries.ts` принимает текущего пользователя и тоже вызывает
  `requirePermission`: страница, в которой забыли проверку, упадёт с ошибкой,
  а не покажет данные.

Интерфейс использует ту же функцию `can()`, чтобы не показывать недоступное.
Дублировать правила отдельным списком для UI нельзя: два списка неизбежно
разойдутся.

## 5. Добавление раздела в следующих этапах

Новый раздел означает добавление строк прав (`projects.read`, `projects.create`, …)
в матрицу и вызовы `requirePermission` в его действиях. Переписывать механизм
проверки не требуется — это и есть причина, по которой права заданы строками,
а не набором булевых полей у роли.

## 6. Что проверять при приёмке

- Менеджер открывает `/users`, но не видит кнопок создания, сброса пароля и
  деактивации; «Редактировать» есть у всех, кроме администраторов, и форма открывает
  ему только ФИО, должность, email и телефон.
- Менеджер, открыв `/users/[id]/edit` администратора, получает «Доступ запрещён»;
  прямой вызов правки администратора или комментария отклоняется сервером.
- Сотрудник и подрядчик открывают `/profile` без формы правки; администратор
  и менеджер правят в нём свои данные.
- Менеджер видит в карточке вкладку «История изменений» без IP-адресов и браузеров,
  а `/audit` для него закрыт.
- Прямой вызов действия создания пользователя от имени менеджера отклоняется
  сервером, а не только скрыт в интерфейсе.
- Сотрудник и подрядчик при переходе на `/users` и `/audit` получают
  «Доступ запрещён».
- Попытка понизить роль последнего администратора отклоняется с понятным
  сообщением.
- Попытка администратора деактивировать самого себя отклоняется.

Этап 2:

- Администратор и менеджер видят пункт меню и карточку «Настройки», открывают `/settings`,
  правят сведения о компании и открывают `/settings/company/history`.
- Сотрудник и подрядчик не видят пункта меню и карточки, а `/settings` и
  `/settings/company/history` показывают им «Доступ запрещён».
- Прямой вызов действия сохранения сведений о компании от имени сотрудника отклоняется
  сервером до проверки данных.
- Менеджер в истории изменений сведений о компании не видит IP-адресов и браузеров.

Этап 3:

- Администратор и менеджер создают и правят заказчиков, подрядчиков и проекты, закрывают
  проекты и возвращают их в работу; удаляет проект только администратор.
- Прямой вызов правки или удаления закрытого проекта отклоняется сервером — от имени
  любой роли.
- Сотрудник открывает реестры и карточки проектов, заказчиков и подрядчиков, историю
  изменений, но не видит бюджета, кнопок правки и выгрузки; прямой вызов создания проекта
  от его имени отклоняется.
- Подрядчик видит только проекты «В работе» без заказчика и бюджета; закрытый проект по
  прямой ссылке даёт «Не найдено»; `/customers` и `/contractors` — «Доступ запрещён».
- Менеджер не видит поля «Организация» в форме пользователя, а прямая передача
  `contractorId` от его имени отклоняется.
- Архивного заказчика нельзя выбрать в новом проекте прямым вызовом действия.

Этап 4:

- Сотрудник и подрядчик видят в «Отчётах» только свои отчёты; чужой отчёт по прямой ссылке —
  «Не найдено», в том числе отчёт коллеги по организации.
- Прямой вызов внесения отчёта от имени подрядчика с чужим `userId` отклоняется; подрядчик
  без организации или с архивной организацией не вносит отчёт прямым вызовом.
- Работник не правит и не удаляет свой утверждённый отчёт прямым вызовом; менеджер не удаляет
  утверждённый отчёт, не сняв утверждение.
- Внесение, правка, удаление, утверждение и снятие утверждения отчёта закрытого проекта
  отклоняются сервером от имени любой роли; закрытие проекта с неутверждёнными отчётами
  и удаление проекта с отчётами — тоже.
- `/reports/import`, выгрузка табеля и вкладка «Участники» дают сотруднику и подрядчику
  «Доступ запрещён»; прямой вызов утверждения и импорта от их имени отклоняется.
- Менеджер не меняет никнейм администратора ни в форме, ни прямым вызовом.
